# Business Brain PRD - AI 기반 경영 인사이트 시스템

## 1. 개요

### 1.1 배경
현재 대시보드는 KPI 표시와 물류 현황 중심으로 구성되어 있으나, 경영 책임자 수준의 **총괄적 비즈니스 평가**와 **전략적 방향 제시** 기능이 부재합니다. 실무자가 단기적 지표만 확인하다 보면 장기적 트렌드나 숨겨진 리스크를 놓칠 수 있으며, 데이터에서 도출 가능한 인사이트가 충분히 활용되지 못하고 있습니다.

### 1.2 목표
- **경영 책임자 역할 수행**: AI가 CEO/COO 관점에서 비즈니스 전체를 조망하고 방향 제시
- **휴먼 에러 방지**: 사람이 놓치기 쉬운 패턴, 이상 징후, 상관관계 자동 감지
- **장기적 관점 제공**: 단기 지표 너머의 트렌드, 시즌성, 구조적 변화 분석
- **실행 가능한 인사이트**: 추상적 분석이 아닌 구체적 액션 아이템 제시

### 1.3 핵심 가치
```
"데이터를 보는 것"에서 "데이터가 말하는 것을 듣는 것"으로의 전환
```

### 1.4 인간 한계를 넘어서는 분석 목표

| 영역 | 인간의 한계 | Business Brain 목표 |
|------|------------|---------------------|
| **조합 분석** | 3-4개 변수 동시 고려 | 수십 개 변수 교차 분석 (수천 개 조합) |
| **패턴 인식** | 최근 데이터 편향 | 전체 기간 일관 분석 |
| **이상 탐지** | 주관적 판단, 놓침 | 모든 데이터 포인트 검사 |
| **시간 분석** | 시간대별 분석 비현실적 | 분/시간/일/주/월 동시 분석 |
| **인과 추론** | 직관적 관계만 인식 | 숨겨진 상관관계 및 선행 지표 발견 |
| **예측** | 경험 기반 추정 | 통계적 모델 기반 예측 |

> 📌 **관련 문서**
> - [고급 분석 엔진 설계](./business-brain-advanced-analytics.md) - N차원 큐브, 인과관계 추론, 네트워크/생존 분석
> - [분석 카탈로그](./business-brain-analysis-catalog.md) - 데이터 인벤토리, 기본 분석 항목
> - [구현 가이드](./business-brain-implementation-guide.md) - 기술 명세, API, 코드 예시, 일정

---

## 2. 데이터 분석 프레임워크

### 2.1 활용 가능한 데이터 소스

| 시트 | 핵심 지표 | 분석 가능 영역 |
|------|----------|---------------|
| **order** | GMV, 주문수, AOV, 플랫폼, 결제수단 | 매출 트렌드, 결제 패턴, 플랫폼 성과 |
| **logistics** | 물류 상태, 배송 국가, 작가별 현황 | 운영 효율성, 지역별 성과, 작가 의존도 |
| **users** | 가입일, 국가, 구매 이력 | 고객 생애주기, RFM, 이탈 예측 |
| **artists** | 작품수, 등록일, 이탈일 | 작가 건강도, 공급 안정성 |
| **review** | 평점, 리뷰 내용, 국가 | 고객 만족도, 품질 이슈 |

### 2.2 데이터 가공 방법론

#### 2.2.1 시계열 분석 (Time Series Analysis)
```typescript
interface TimeSeriesAnalysis {
  // 일별/주별/월별 집계
  dailyAggregation: { date: string; gmv: number; orders: number; aov: number }[]
  weeklyAggregation: { week: string; gmv: number; orders: number; aov: number }[]
  monthlyAggregation: { month: string; gmv: number; orders: number; aov: number }[]
  
  // 이동평균 (7일, 30일)
  movingAverage7d: number[]
  movingAverage30d: number[]
  
  // 전기 대비 변화율
  periodOverPeriod: {
    wow: number  // Week over Week
    mom: number  // Month over Month
    yoy: number  // Year over Year
  }
}
```


#### 2.2.2 코호트 분석 (Cohort Analysis)
```typescript
interface CohortAnalysis {
  // 가입 월별 코호트
  cohorts: {
    cohortMonth: string
    totalUsers: number
    retentionByMonth: number[]  // [M0, M1, M2, ...]
    ltv: number
    avgOrdersPerUser: number
  }[]
  
  // 첫 구매 전환율
  firstPurchaseConversion: {
    cohortMonth: string
    registered: number
    converted: number
    conversionRate: number
    avgDaysToFirstPurchase: number
  }[]
}
```

#### 2.2.3 RFM 세분화 (RFM Segmentation)
```typescript
interface RFMAnalysis {
  segments: {
    segment: 'VIP' | 'Loyal' | 'Potential' | 'New' | 'AtRisk' | 'Dormant' | 'Lost'
    count: number
    percentage: number
    avgRecency: number
    avgFrequency: number
    avgMonetary: number
    totalRevenue: number
  }[]
  
  // 세그먼트 이동 추적
  segmentMigration: {
    from: string
    to: string
    count: number
    period: string
  }[]
}
```

#### 2.2.4 파레토 분석 (Pareto Analysis)
```typescript
interface ParetoAnalysis {
  // 작가 집중도
  artistConcentration: {
    top10Percent: { count: number; revenueShare: number }
    top20Percent: { count: number; revenueShare: number }
    bottom50Percent: { count: number; revenueShare: number }
  }
  
  // 상품 집중도
  productConcentration: {
    top10Products: { products: string[]; revenueShare: number }
    longTail: { count: number; revenueShare: number }
  }
  
  // 국가 집중도
  countryConcentration: {
    primary: { country: string; share: number }
    secondary: { countries: string[]; share: number }
  }
}
```

#### 2.2.5 상관관계 분석 (Correlation Analysis)
```typescript
interface CorrelationAnalysis {
  // 변수 간 상관관계
  correlations: {
    variable1: string
    variable2: string
    coefficient: number  // -1 ~ 1
    significance: 'high' | 'medium' | 'low'
    interpretation: string
  }[]
  
  // 선행 지표 발견
  leadingIndicators: {
    indicator: string
    outcome: string
    lagDays: number
    correlation: number
  }[]
}
```

#### 2.2.6 이상 탐지 (Anomaly Detection)
```typescript
interface AnomalyDetection {
  anomalies: {
    date: string
    metric: string
    actualValue: number
    expectedValue: number
    deviation: number  // 표준편차 기준
    severity: 'critical' | 'warning' | 'info'
    possibleCauses: string[]
  }[]
  
  // 패턴 이탈
  patternBreaks: {
    metric: string
    breakDate: string
    beforePattern: string
    afterPattern: string
    significance: number
  }[]
}
```

---

## 3. 분석 방법론 및 인사이트 도출

### 3.1 다차원 분석 매트릭스

| 분석 차원 | 단기 (7일) | 중기 (30일) | 장기 (90일+) |
|----------|-----------|------------|-------------|
| **매출** | 일별 변동성, 요일 패턴 | 주간 트렌드, 프로모션 효과 | 시즌성, 성장률 |
| **고객** | 신규 유입, 재방문 | 전환율, 재구매율 | LTV, 이탈률 |
| **작가** | 발송 지연, 품질 이슈 | 매출 변동, 신규 등록 | 이탈 위험, 의존도 |
| **물류** | 파이프라인 병목 | 평균 처리 시간 | 국가별 효율성 |
| **상품** | 베스트셀러 변화 | 카테고리 트렌드 | 롱테일 분포 |

### 3.2 인사이트 유형 분류

#### Type A: 즉시 조치 필요 (Immediate Action Required)
```
🚨 긴급 알림
- 14일+ 미입고 급증 (전주 대비 +150%)
- 특정 작가 발송 지연 집중 (작가 A: 8건)
- 결제 실패율 이상 상승 (평균 2% → 8%)
```

#### Type B: 주의 관찰 필요 (Watch & Monitor)
```
⚠️ 주의 사항
- 일본 매출 3주 연속 하락 (-5%, -7%, -9%)
- VIP 고객 재구매 주기 늘어남 (45일 → 62일)
- 신규 작가 등록 감소 추세
```

#### Type C: 기회 포착 (Opportunity Identified)
```
💡 기회 발견
- 대만 시장 급성장 (MoM +45%)
- 특정 카테고리 수요 증가 (도자기 +30%)
- 주말 주문 비중 상승 (마케팅 타이밍 조정 권장)
```

#### Type D: 구조적 변화 감지 (Structural Change)
```
📊 트렌드 변화
- AOV 장기 하락 추세 (6개월간 -12%)
- 모바일 비중 지속 상승 (65% → 78%)
- 결제 수단 선호도 변화 (카드 → 간편결제)
```

### 3.3 휴먼 에러 방지 체크리스트

| 놓치기 쉬운 항목 | 자동 감지 방법 | 알림 조건 |
|----------------|--------------|----------|
| 특정 작가 의존도 과다 | 파레토 분석 | 상위 1명 > 15% |
| 국가 집중 리스크 | 국가별 비중 추적 | 단일 국가 > 60% |
| 시즌성 미반영 | YoY 비교 | 전년 동기 대비 ±30% |
| 고객 이탈 조기 징후 | RFM 이동 추적 | VIP→AtRisk 이동 증가 |
| 물류 병목 누적 | 파이프라인 체류 시간 | 평균 +50% 초과 |
| 품질 이슈 확산 | 리뷰 감성 분석 | 부정 리뷰 비율 급증 |

---

## 4. Business Brain 기능 설계

### 4.1 핵심 기능 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                    🧠 Business Brain                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ 📊 현황     │  │ 🔮 예측     │  │ 💡 제안     │             │
│  │ Assessment  │  │ Forecast    │  │ Recommend   │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│         │                │                │                     │
│         ▼                ▼                ▼                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              🎯 Executive Summary                        │   │
│  │  "현재 비즈니스는 [상태]이며, [핵심 이슈]에 주목해야     │   │
│  │   합니다. 단기적으로 [액션1], 중기적으로 [액션2]를       │   │
│  │   권장합니다."                                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 대시보드 통합 UI

```
┌─────────────────────────────────────────────────────────────────┐
│ 🧠 Business Brain                              [상세 분석 →]    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 📈 비즈니스 건강도: ████████░░ 78/100 (양호)                   │
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ 💬 AI 경영 브리핑                                           ││
│ │                                                             ││
│ │ "이번 주 매출은 전주 대비 12% 성장했으나, 일본 시장의      ││
│ │  성장 둔화(-5%)가 우려됩니다. 대만 시장이 빠르게 성장      ││
│ │  중이므로 마케팅 리소스 재배분을 검토하세요.               ││
│ │                                                             ││
│ │  🚨 즉시 확인: 작가 A의 발송 지연 8건 (고객 불만 예상)     ││
│ │  💡 기회: 주말 프로모션 효과 +23% (지속 권장)              ││
│ │  ⚠️ 주의: VIP 고객 3명 60일+ 미구매 (리텐션 필요)         ││
│ │                                                             ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ [📊 상세 분석] [🔮 예측 보기] [💡 전략 제안] [📝 리포트 생성] │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```


### 4.3 상세 분석 페이지 구조

```
┌─────────────────────────────────────────────────────────────────┐
│ 🧠 Business Brain - 상세 분석                                   │
├─────────────────────────────────────────────────────────────────┤
│ [현황 평가] [트렌드 분석] [리스크 감지] [기회 발견] [전략 제안] │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ 📊 종합 현황 평가                                           ││
│ │                                                             ││
│ │ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐            ││
│ │ │ 매출    │ │ 고객    │ │ 작가    │ │ 운영    │            ││
│ │ │ 건강도  │ │ 건강도  │ │ 건강도  │ │ 효율성  │            ││
│ │ │ 82/100  │ │ 75/100  │ │ 68/100  │ │ 85/100  │            ││
│ │ │   ↑5    │ │   ↓3    │ │   ↓8    │ │   ↑2    │            ││
│ │ └─────────┘ └─────────┘ └─────────┘ └─────────┘            ││
│ │                                                             ││
│ │ 종합 점수: 78/100 (전주 대비 +1)                           ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐│
│ │ 🔍 핵심 발견 사항                                           ││
│ │                                                             ││
│ │ 1. 매출 성장 지속 중이나 AOV 하락 추세 (-3% MoM)           ││
│ │    → 저가 상품 비중 증가 또는 할인 의존도 점검 필요        ││
│ │                                                             ││
│ │ 2. 신규 고객 유입 증가 (+15%) but 재구매율 정체 (32%)      ││
│ │    → 첫 구매 후 리텐션 프로그램 강화 권장                  ││
│ │                                                             ││
│ │ 3. 상위 5명 작가가 전체 매출의 45% 차지 (집중 리스크)      ││
│ │    → 중위권 작가 육성 및 신규 작가 발굴 필요               ││
│ │                                                             ││
│ └─────────────────────────────────────────────────────────────┘│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 기술 구현 설계

### 5.1 새로운 Agent: BusinessBrainAgent

```typescript
// backend/src/services/agents/BusinessBrainAgent.ts

interface BusinessHealthScore {
  overall: number
  dimensions: {
    revenue: { score: number; trend: 'up' | 'down' | 'stable'; change: number }
    customer: { score: number; trend: 'up' | 'down' | 'stable'; change: number }
    artist: { score: number; trend: 'up' | 'down' | 'stable'; change: number }
    operations: { score: number; trend: 'up' | 'down' | 'stable'; change: number }
  }
}

interface BusinessInsight {
  id: string
  type: 'immediate' | 'watch' | 'opportunity' | 'structural'
  severity: 'critical' | 'warning' | 'info' | 'positive'
  title: string
  description: string
  evidence: {
    metric: string
    currentValue: number
    comparisonValue: number
    changePercent: number
  }[]
  recommendation: string
  actionLink?: string
  priority: number
}

interface ExecutiveBriefing {
  generatedAt: string
  period: { start: string; end: string }
  healthScore: BusinessHealthScore
  summary: string  // AI 생성 요약
  insights: BusinessInsight[]
  immediateActions: string[]
  weeklyFocus: string[]
  risks: string[]
  opportunities: string[]
}

export class BusinessBrainAgent extends BaseAgent {
  /**
   * 종합 비즈니스 평가 생성
   */
  async generateExecutiveBriefing(
    period?: { start: string; end: string }
  ): Promise<ExecutiveBriefing>

  /**
   * 비즈니스 건강도 점수 계산
   */
  async calculateHealthScore(): Promise<BusinessHealthScore>

  /**
   * 인사이트 자동 발견
   */
  async discoverInsights(): Promise<BusinessInsight[]>

  /**
   * 휴먼 에러 체크
   */
  async runHumanErrorChecks(): Promise<{
    checks: Array<{
      name: string
      status: 'pass' | 'warning' | 'fail'
      message: string
    }>
  }>

  /**
   * 장기 트렌드 분석
   */
  async analyzeLongTermTrends(): Promise<{
    trends: Array<{
      metric: string
      direction: 'up' | 'down' | 'stable'
      magnitude: number
      significance: string
      implication: string
    }>
  }>

  /**
   * 전략 제안 생성
   */
  async generateStrategicRecommendations(): Promise<{
    shortTerm: string[]  // 1-2주
    midTerm: string[]    // 1-3개월
    longTerm: string[]   // 3개월+
  }>
}
```

### 5.2 데이터 처리 파이프라인

```typescript
// backend/src/services/analytics/DataProcessor.ts

export class DataProcessor {
  /**
   * 시계열 데이터 가공
   */
  async processTimeSeries(
    data: any[],
    dateColumn: string,
    valueColumns: string[],
    granularity: 'daily' | 'weekly' | 'monthly'
  ): Promise<TimeSeriesData>

  /**
   * 코호트 분석 실행
   */
  async runCohortAnalysis(
    users: any[],
    orders: any[],
    cohortColumn: string
  ): Promise<CohortAnalysis>

  /**
   * RFM 세분화 실행
   */
  async runRFMSegmentation(
    orders: any[],
    config: RFMConfig
  ): Promise<RFMAnalysis>

  /**
   * 파레토 분석 실행
   */
  async runParetoAnalysis(
    data: any[],
    groupColumn: string,
    valueColumn: string
  ): Promise<ParetoAnalysis>

  /**
   * 이상 탐지 실행
   */
  async detectAnomalies(
    timeSeries: TimeSeriesData,
    sensitivity: 'low' | 'medium' | 'high'
  ): Promise<AnomalyDetection>

  /**
   * 상관관계 분석 실행
   */
  async analyzeCorrelations(
    data: any[],
    columns: string[]
  ): Promise<CorrelationAnalysis>
}
```

### 5.3 API 엔드포인트

```typescript
// backend/src/routes/business-brain.ts

// GET /api/business-brain/briefing
// 경영 브리핑 조회
interface BriefingResponse {
  briefing: ExecutiveBriefing
  lastUpdated: string
  nextUpdate: string
}

// GET /api/business-brain/health-score
// 비즈니스 건강도 점수
interface HealthScoreResponse {
  score: BusinessHealthScore
  history: Array<{ date: string; score: number }>
}

// GET /api/business-brain/insights
// 인사이트 목록
interface InsightsResponse {
  insights: BusinessInsight[]
  filters: {
    types: string[]
    severities: string[]
  }
}

// GET /api/business-brain/trends
// 장기 트렌드 분석
interface TrendsResponse {
  trends: TrendAnalysis[]
  period: { start: string; end: string }
}

// GET /api/business-brain/recommendations
// 전략 제안
interface RecommendationsResponse {
  recommendations: {
    shortTerm: Recommendation[]
    midTerm: Recommendation[]
    longTerm: Recommendation[]
  }
}

// POST /api/business-brain/ask
// AI에게 비즈니스 질문
interface AskRequest {
  question: string
  context?: {
    period?: { start: string; end: string }
    focus?: 'revenue' | 'customer' | 'artist' | 'operations'
  }
}
```

---

## 6. 건강도 점수 계산 로직

### 6.1 매출 건강도 (Revenue Health)

```typescript
function calculateRevenueHealth(data: RevenueData): number {
  let score = 50 // 기본 점수

  // 1. 성장률 (+/- 20점)
  const growthRate = data.mom // Month over Month
  if (growthRate > 0.15) score += 20
  else if (growthRate > 0.05) score += 10
  else if (growthRate > 0) score += 5
  else if (growthRate > -0.05) score -= 5
  else if (growthRate > -0.15) score -= 10
  else score -= 20

  // 2. AOV 트렌드 (+/- 10점)
  const aovTrend = data.aovChange
  if (aovTrend > 0.05) score += 10
  else if (aovTrend > 0) score += 5
  else if (aovTrend > -0.05) score -= 5
  else score -= 10

  // 3. 변동성 (+/- 10점)
  const volatility = data.dailyVolatility
  if (volatility < 0.1) score += 10
  else if (volatility < 0.2) score += 5
  else if (volatility > 0.3) score -= 10

  // 4. 목표 달성률 (+/- 10점)
  const targetAchievement = data.actualVsTarget
  if (targetAchievement >= 1.0) score += 10
  else if (targetAchievement >= 0.9) score += 5
  else if (targetAchievement < 0.7) score -= 10

  return Math.max(0, Math.min(100, score))
}
```

### 6.2 고객 건강도 (Customer Health)

```typescript
function calculateCustomerHealth(data: CustomerData): number {
  let score = 50

  // 1. 신규 고객 유입 (+/- 15점)
  const newCustomerGrowth = data.newCustomerGrowth
  if (newCustomerGrowth > 0.2) score += 15
  else if (newCustomerGrowth > 0.1) score += 10
  else if (newCustomerGrowth > 0) score += 5
  else score -= 10

  // 2. 재구매율 (+/- 15점)
  const repeatRate = data.repeatPurchaseRate
  if (repeatRate > 0.4) score += 15
  else if (repeatRate > 0.3) score += 10
  else if (repeatRate > 0.2) score += 5
  else score -= 10

  // 3. VIP 유지율 (+/- 10점)
  const vipRetention = data.vipRetentionRate
  if (vipRetention > 0.9) score += 10
  else if (vipRetention > 0.8) score += 5
  else if (vipRetention < 0.7) score -= 10

  // 4. 이탈 위험 고객 비율 (+/- 10점)
  const atRiskRatio = data.atRiskCustomerRatio
  if (atRiskRatio < 0.1) score += 10
  else if (atRiskRatio < 0.2) score += 5
  else if (atRiskRatio > 0.3) score -= 10

  return Math.max(0, Math.min(100, score))
}
```

### 6.3 작가 건강도 (Artist Health)

```typescript
function calculateArtistHealth(data: ArtistData): number {
  let score = 50

  // 1. 활성 작가 수 변화 (+/- 15점)
  const activeArtistChange = data.activeArtistGrowth
  if (activeArtistChange > 0.1) score += 15
  else if (activeArtistChange > 0) score += 5
  else if (activeArtistChange > -0.05) score -= 5
  else score -= 15

  // 2. 집중도 리스크 (+/- 15점)
  const top5Share = data.top5ArtistRevenueShare
  if (top5Share < 0.3) score += 15
  else if (top5Share < 0.4) score += 5
  else if (top5Share > 0.5) score -= 15

  // 3. 이탈 위험 작가 (+/- 10점)
  const atRiskArtists = data.atRiskArtistCount
  if (atRiskArtists === 0) score += 10
  else if (atRiskArtists <= 2) score += 5
  else if (atRiskArtists > 5) score -= 10

  // 4. 신규 작가 온보딩 (+/- 10점)
  const newArtists = data.newArtistsThisMonth
  if (newArtists >= 5) score += 10
  else if (newArtists >= 2) score += 5
  else if (newArtists === 0) score -= 5

  return Math.max(0, Math.min(100, score))
}
```

### 6.4 운영 효율성 (Operations Efficiency)

```typescript
function calculateOperationsHealth(data: OperationsData): number {
  let score = 50

  // 1. 평균 처리 시간 (+/- 15점)
  const avgProcessingDays = data.avgOrderToDeliveryDays
  if (avgProcessingDays < 7) score += 15
  else if (avgProcessingDays < 10) score += 10
  else if (avgProcessingDays < 14) score += 5
  else if (avgProcessingDays > 21) score -= 15

  // 2. 지연 건수 비율 (+/- 15점)
  const delayRatio = data.delayedOrderRatio
  if (delayRatio < 0.05) score += 15
  else if (delayRatio < 0.1) score += 10
  else if (delayRatio > 0.2) score -= 15

  // 3. 검수 통과율 (+/- 10점)
  const qcPassRate = data.qcPassRate
  if (qcPassRate > 0.95) score += 10
  else if (qcPassRate > 0.9) score += 5
  else if (qcPassRate < 0.85) score -= 10

  // 4. 고객 불만 비율 (+/- 10점)
  const complaintRatio = data.customerComplaintRatio
  if (complaintRatio < 0.01) score += 10
  else if (complaintRatio < 0.02) score += 5
  else if (complaintRatio > 0.05) score -= 10

  return Math.max(0, Math.min(100, score))
}
```


---

## 7. AI 브리핑 생성 프롬프트

### 7.1 Executive Summary 생성

```typescript
const executiveSummaryPrompt = `
당신은 글로벌 이커머스 비즈니스의 경영 고문입니다.
다음 데이터를 바탕으로 경영진에게 보고할 수 있는 수준의 브리핑을 작성하세요.

## 분석 기간
${period.start} ~ ${period.end}

## 핵심 지표
- 총 매출: ${metrics.totalGmv} USD (전기 대비 ${metrics.gmvChange}%)
- 주문 건수: ${metrics.orderCount}건 (전기 대비 ${metrics.orderChange}%)
- AOV: ${metrics.aov} USD (전기 대비 ${metrics.aovChange}%)
- 신규 고객: ${metrics.newCustomers}명
- 재구매율: ${metrics.repeatRate}%

## 건강도 점수
- 종합: ${healthScore.overall}/100
- 매출: ${healthScore.dimensions.revenue.score}/100 (${healthScore.dimensions.revenue.trend})
- 고객: ${healthScore.dimensions.customer.score}/100 (${healthScore.dimensions.customer.trend})
- 작가: ${healthScore.dimensions.artist.score}/100 (${healthScore.dimensions.artist.trend})
- 운영: ${healthScore.dimensions.operations.score}/100 (${healthScore.dimensions.operations.trend})

## 발견된 이상 징후
${anomalies.map(a => `- ${a.metric}: ${a.description}`).join('\n')}

## 주요 트렌드
${trends.map(t => `- ${t.metric}: ${t.direction} (${t.magnitude}%)`).join('\n')}

## 작성 지침
1. 첫 문장에서 전체 비즈니스 상태를 한 줄로 요약
2. 가장 중요한 이슈 2-3개를 우선순위 순으로 언급
3. 즉시 조치가 필요한 항목은 🚨로 표시
4. 기회 요인은 💡로 표시
5. 주의 관찰 항목은 ⚠️로 표시
6. 구체적인 숫자와 비교 기준을 포함
7. 마지막에 이번 주 집중해야 할 3가지 제안

응답은 한국어로, 300자 이내로 작성하세요.
`
```

### 7.2 인사이트 해석 프롬프트

```typescript
const insightInterpretationPrompt = `
다음 데이터 패턴에 대해 비즈니스 관점의 해석을 제공하세요.

## 발견된 패턴
${pattern.description}

## 관련 데이터
${JSON.stringify(pattern.data, null, 2)}

## 해석 요청
1. 이 패턴이 의미하는 바는 무엇인가?
2. 발생 가능한 원인은 무엇인가?
3. 비즈니스에 미치는 영향은?
4. 권장하는 대응 방안은?

간결하고 실행 가능한 형태로 답변하세요.
`
```

---

## 8. 구현 로드맵

### Phase 1: 데이터 가공 인프라 (1주)

| 작업 | 설명 | 우선순위 |
|------|------|---------|
| DataProcessor 클래스 구현 | 시계열, 코호트, RFM 분석 | P0 |
| 이상 탐지 모듈 구현 | Z-score 기반 이상치 감지 | P0 |
| 상관관계 분석 모듈 확장 | 기존 CorrelationAnalyzer 확장 | P1 |
| 캐싱 전략 수립 | 분석 결과 캐싱 (10분) | P1 |

### Phase 2: BusinessBrainAgent 구현 (1주)

| 작업 | 설명 | 우선순위 |
|------|------|---------|
| 건강도 점수 계산 로직 | 4개 차원 점수 계산 | P0 |
| 인사이트 자동 발견 | 규칙 기반 + AI 해석 | P0 |
| Executive Briefing 생성 | LLM 기반 요약 생성 | P0 |
| 휴먼 에러 체크 로직 | 체크리스트 자동 검증 | P1 |

### Phase 3: API 및 프론트엔드 (1주)

| 작업 | 설명 | 우선순위 |
|------|------|---------|
| API 엔드포인트 구현 | /api/business-brain/* | P0 |
| 대시보드 위젯 추가 | Business Brain 미니뷰 | P0 |
| 상세 분석 페이지 | /business-brain 페이지 | P1 |
| 알림 시스템 연동 | 긴급 인사이트 알림 | P2 |

### Phase 4: 고도화 (1주)

| 작업 | 설명 | 우선순위 |
|------|------|---------|
| 예측 모델 추가 | 매출/주문 예측 | P1 |
| 시나리오 시뮬레이션 | What-if 분석 | P2 |
| 자동 리포트 생성 | 주간/월간 리포트 PDF | P2 |
| 개인화 설정 | 관심 지표 설정 | P3 |

---

## 9. 성공 지표

### 9.1 정량적 지표

| 지표 | 현재 | 목표 | 측정 방법 |
|------|------|------|----------|
| 인사이트 활용률 | - | 70%+ | 인사이트 클릭 → 액션 전환 |
| 이슈 조기 발견율 | - | 80%+ | AI 감지 vs 수동 발견 비교 |
| 브리핑 확인 빈도 | - | 일 1회+ | 대시보드 접속 로그 |
| 의사결정 시간 단축 | - | 30%+ | 이슈 발견 → 조치 시간 |

### 9.2 정성적 지표

- 경영진이 대시보드만으로 전체 현황 파악 가능
- 숨겨진 리스크/기회를 사전에 인지
- 데이터 기반 의사결정 문화 정착
- 휴먼 에러로 인한 이슈 감소

---

## 10. 리스크 및 대응

### 10.1 기술적 리스크

| 리스크 | 영향 | 대응 방안 |
|--------|------|----------|
| LLM 응답 품질 불안정 | 잘못된 인사이트 제공 | 규칙 기반 검증 + 신뢰도 표시 |
| 데이터 처리 지연 | 실시간성 저하 | 캐싱 + 비동기 처리 |
| 과도한 알림 | 알림 피로 | 중요도 필터링 + 집계 |

### 10.2 비즈니스 리스크

| 리스크 | 영향 | 대응 방안 |
|--------|------|----------|
| AI 의존도 과다 | 비판적 사고 감소 | "AI 제안" 명시 + 근거 제공 |
| 잘못된 예측 | 의사결정 오류 | 신뢰 구간 표시 + 면책 조항 |
| 데이터 편향 | 편향된 인사이트 | 다각도 분석 + 한계 명시 |

---

## 11. 부록

### 11.1 용어 정의

| 용어 | 정의 |
|------|------|
| 건강도 점수 | 비즈니스 상태를 0-100으로 수치화한 지표 |
| 인사이트 | 데이터에서 도출된 비즈니스 시사점 |
| 휴먼 에러 체크 | 사람이 놓치기 쉬운 항목 자동 검증 |
| Executive Briefing | 경영진용 요약 보고서 |

### 11.2 관련 문서

- [대시보드 재구성 PRD](./prd-dashboard-redesign.md)
- [작가 분석 PRD](./prd-artist-analytics.md)
- [데이터 구조 문서](../raw_data_structure.md)

### 11.3 참고 시스템

- 기존 BusinessManagerAgent
- DataAnalystAgent
- AgentOrchestrator
- CorrelationAnalyzer

---

## 12. 분석 카탈로그 연동

본 PRD의 상세 분석 방법론은 별도 문서로 관리됩니다.

### 12.1 관련 문서
- **[Business Brain 분석 카탈로그](./business-brain-analysis-catalog.md)**: 전체 분석 방법론 및 데이터 인벤토리

### 12.2 핵심 원칙

#### 분석은 포괄적으로, 노출은 선택적으로
```
┌─────────────────────────────────────────────────────────────┐
│                    분석 실행 (Background)                    │
│  • 100+ 개의 분석 항목 상시 실행                            │
│  • 모든 데이터 조합에 대한 이상 탐지                        │
│  • 통계적 유의성 검증                                       │
├─────────────────────────────────────────────────────────────┤
│                    특이점 필터링                             │
│  • Z-Score > 2 (95% 신뢰구간 외)                           │
│  • 비즈니스 영향도 임계값 초과                              │
│  • 연속성/지속성 검증                                       │
├─────────────────────────────────────────────────────────────┤
│                    사용자 노출 (Frontend)                    │
│  • Critical: 즉시 알림 (빨간색)                             │
│  • Warning: 대시보드 상단 (주황색)                          │
│  • Opportunity: 기회 섹션 (초록색)                          │
│  • Info: 참고 섹션 (회색, 접힘)                             │
└─────────────────────────────────────────────────────────────┘
```

#### 휴먼 에러 방지 자동 체크
| 체크 항목 | 감지 조건 | 알림 수준 |
|----------|----------|----------|
| 작가 집중도 과다 | Top 1 작가 > 15% | Warning |
| 국가 집중도 과다 | 단일 국가 > 60% | Warning |
| VIP 이탈 징후 | VIP→AtRisk 이동 증가 | Warning |
| 시즌성 미반영 | YoY 대비 ±30% | Info |
| 물류 병목 누적 | 체류 시간 +50% | Warning |
| 품질 이슈 확산 | 저평점 비율 급증 | Critical |

---

## 13. 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|----------|
| 1.0 | 2024-12-04 | AI | 초안 작성 |
| 1.1 | 2024-12-04 | AI | 분석 카탈로그 연동, 특이점 기반 노출 전략 추가 |
| 2.0 | 2024-12-04 | AI | 미구현 영역 구현 완료 - DataProcessor, 코호트/RFM/파레토/상관관계/이상탐지 분석, 휴먼에러체크 9개 항목 완성, 프론트엔드 UI 확장 |
| 2.1 | 2024-12-04 | AI | 기간 선택 기능, 매출 예측 모델, 복합 기간 비교 분석, LLM 기반 AI 브리핑 생성 추가 |

---

## 14. 구현 현황 (v2.0)

### 14.1 구현 완료 항목

| 영역 | 구현 상태 | 비고 |
|------|----------|------|
| **건강도 점수 계산** | ✅ 완료 | 4차원 실제 데이터 기반 계산 |
| **Executive Briefing** | ✅ 완료 | AI 요약, 즉시조치/기회/리스크 분류 |
| **휴먼 에러 체크** | ✅ 완료 | 9개 항목 (VIP 이탈, 물류 병목, 품질 이슈 포함) |
| **장기 트렌드 분석** | ✅ 완료 | 90일 다차원 분석 |
| **전략 제안** | ✅ 완료 | 단기/중기/장기 구분 |
| **N차원 큐브 분석** | ✅ 완료 | 3차원 (country, platform, artist) |
| **코호트 분석** | ✅ 완료 | 월별 코호트, 리텐션, LTV |
| **RFM 세분화** | ✅ 완료 | 7개 세그먼트, 이탈 위험 VIP 추출 |
| **파레토 분석** | ✅ 완료 | 작가/국가/고객 집중도, 지니계수, HHI |
| **상관관계 분석** | ✅ 완료 | 피어슨 상관계수, 선행 지표 탐지 |
| **이상 탐지** | ✅ 완료 | Z-score, 패턴 이탈, 트렌드 변화 감지 |
| **시계열 분석** | ✅ 완료 | 일/주/월별 집계, 이동평균, WoW/MoM/YoY |
| **대시보드 통합 위젯** | ✅ 완료 | 미니뷰 + 상세 분석 링크 |
| **프론트엔드 UI** | ✅ 완료 | 9개 탭 (기존 5개 + 신규 4개) |

### 14.2 신규 추가 파일

```
backend/src/services/analytics/
├── DataProcessor.ts          # 코호트/RFM/파레토/상관관계/이상탐지 분석
├── HealthScoreCalculator.ts  # 실제 데이터 기반 건강도 계산 (v2.0)
├── CubeAnalyzer.ts
├── DecompositionEngine.ts
├── InsightScorer.ts
├── BusinessBrainCache.ts
├── types.ts
└── index.tsbackend/src/routes/
└── business-brain.ts         # 신규 API 엔드포인트 추가

frontend/app/business-brain/
└── page.tsx                  # 신규 분석 탭 4개 추가
```

### 14.3 신규 API 엔드포인트

| 엔드포인트 | 설명 |
|-----------|------|
| `GET /api/business-brain/cohort` | 코호트 분석 |
| `GET /api/business-brain/rfm` | RFM 세분화 |
| `GET /api/business-brain/pareto` | 파레토 분석 |
| `GET /api/business-brain/correlation` | 상관관계 분석 |
| `GET /api/business-brain/anomaly` | 이상 탐지 |
| `GET /api/business-brain/timeseries` | 시계열 분석 |
| `GET /api/business-brain/advanced` | 종합 고급 분석 |

### 14.4 v2.1 신규 기능 (2024-12-04)

| 기능 | 설명 |
|------|------|
| **기간 선택 기능** | 7일/30일/90일/180일/365일 프리셋 및 사용자 정의 기간 지원 |
| **매출 예측 모델** | Holt-Winters 기반 30일 예측, 시즌성 감지, 신뢰 구간 |
| **복합 기간 비교** | 두 기간 비교 분석, 다중 기간 트렌드 분석 |
| **LLM 기반 브리핑** | OpenAI GPT-4o-mini 활용 자연어 브리핑 (폴백: 템플릿) |
| **종합 인사이트 탭** | 기간별 요약, 비교, 예측, 인사이트 통합 뷰 |
| **기간별 추이 탭** | 주간/월간/분기별 트렌드 시각화 |
| **매출 예측 탭** | 예측 차트, 신뢰 구간, 시즌성 정보 |

### 14.5 신규 API 엔드포인트 (v2.1)

| 엔드포인트 | 설명 |
|-----------|------|
| `GET /api/business-brain/analysis/:type` | 기간별 분석 (period 쿼리 지원) |
| `GET /api/business-brain/forecast` | 매출 예측 |
| `GET /api/business-brain/compare` | 기간 비교 분석 |
| `GET /api/business-brain/multi-period` | 다중 기간 트렌드 |
| `GET /api/business-brain/comprehensive` | 종합 인사이트 |

### 14.6 향후 개선 과제

| 항목 | 우선순위 | 설명 |
|------|---------|------|
| What-if 시뮬레이션 | P1 | 가격/마케팅 변경 시 영향 예측 |
| 자동 리포트 생성 | P2 | 주간/월간 PDF 리포트 |
| 알림 시스템 | P2 | 긴급 인사이트 실시간 알림 |
| 대시보드 커스터마이징 | P3 | 사용자별 관심 지표 설정 |