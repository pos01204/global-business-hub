# 글로벌 비즈니스팀 온보딩 업무 안내 페이지 구축 가이드

## 📋 프로젝트 개요

노션에 정리된 정책 및 문서를 학습하여 **글로벌 비즈니스팀 신규 입사자를 위한 온보딩 업무 안내 페이지**를 구축하는 프로젝트입니다.

### 목표
- 노션 문서 자동 학습 및 정리
- 신규 입사자가 빠르게 업무를 파악할 수 있는 통합 안내 페이지 생성
- Agent 기반 자동화로 지속적인 업데이트 가능

---

## 🏗️ 전체 구현 아키텍처

```
┌────────────────────────────────────────────────────────────────────────┐
│                          전체 워크플로우                                 │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│   [1단계: 노션 연동]                                                    │
│        │                                                               │
│        ▼                                                               │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐             │
│   │ Notion API  │────▶│ 문서 수집   │────▶│ 원본 저장   │             │
│   │ 연결 설정   │     │ (페이지/DB) │     │ (JSON/MD)   │             │
│   └─────────────┘     └─────────────┘     └─────────────┘             │
│                                                                        │
│   [2단계: 문서 분석 및 정리]                                            │
│        │                                                               │
│        ▼                                                               │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐             │
│   │ AI Agent    │────▶│ 카테고리    │────▶│ 구조화된    │             │
│   │ 문서 분석   │     │ 분류        │     │ 정보 생성   │             │
│   └─────────────┘     └─────────────┘     └─────────────┘             │
│                                                                        │
│   [3단계: 온보딩 페이지 생성]                                           │
│        │                                                               │
│        ▼                                                               │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐             │
│   │ 콘텐츠 구성 │────▶│ UI/UX 설계  │────▶│ 페이지 구현 │             │
│   │ 기획        │     │             │     │             │             │
│   └─────────────┘     └─────────────┘     └─────────────┘             │
│                                                                        │
│   [4단계: 배포 및 운영]                                                 │
│        │                                                               │
│        ▼                                                               │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐             │
│   │ 테스트 검증 │────▶│ 배포        │────▶│ 자동 동기화 │             │
│   └─────────────┘     └─────────────┘     └─────────────┘             │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 📌 구현 단계별 상세 가이드

### 1단계: 노션 연동 및 문서 수집

#### 1.1 사전 준비사항

| 항목 | 설명 | 상태 |
|------|------|------|
| Notion API 연동 | 백엔드에 Notion 서비스 구현 완료 | ✅ 완료 |
| API 키 설정 | `NOTION_API_KEY` 환경변수 설정 | ⏳ 확인 필요 |
| 페이지 공유 | 학습할 페이지들을 통합에 공유 | ⏳ 작업 필요 |

#### 1.2 노션 페이지 공유 작업

1. **Notion 통합 생성** (이미 완료된 경우 스킵)
   - https://www.notion.so/my-integrations 접속
   - "Global Business Hub" 등 이름으로 통합 생성
   - Internal Integration Token 복사

2. **학습할 문서 목록 선정**
   - 글로벌 비즈니스팀 업무 매뉴얼
   - 정책 문서 (배송, CS, 운영 등)
   - 시스템 사용 가이드
   - FAQ 및 참고자료

3. **문서 공유 설정**
   - 각 페이지에서 "공유" → 통합 추가
   - 데이터베이스의 경우 DB 페이지에서 통합 추가

#### 1.3 문서 수집 스크립트

```typescript
// backend/src/services/onboardingCollector.ts
import { NotionService } from './notionService';

interface CollectedDocument {
  id: string;
  title: string;
  category: string;
  content: string;
  lastUpdated: string;
  url: string;
}

export class OnboardingCollector {
  private notionService: NotionService;
  private targetPageIds: string[];
  
  constructor(pageIds: string[]) {
    this.notionService = new NotionService();
    this.targetPageIds = pageIds;
  }
  
  // 모든 지정된 페이지에서 문서 수집
  async collectAllDocuments(): Promise<CollectedDocument[]> {
    const documents: CollectedDocument[] = [];
    
    for (const pageId of this.targetPageIds) {
      try {
        const learningData = await this.notionService.getPageLearningData(pageId);
        documents.push({
          id: learningData.id,
          title: learningData.title,
          category: this.categorizeDocument(learningData.title),
          content: learningData.text,
          lastUpdated: learningData.metadata?.lastEditedTime || '',
          url: learningData.metadata?.url || ''
        });
      } catch (error) {
        console.error(`Failed to collect page ${pageId}:`, error);
      }
    }
    
    return documents;
  }
  
  // 문서 제목 기반 카테고리 자동 분류
  private categorizeDocument(title: string): string {
    const categories = {
      '업무 프로세스': ['프로세스', '절차', '워크플로우', '업무'],
      '정책': ['정책', '규정', '가이드라인', '규칙'],
      '시스템 가이드': ['시스템', '사용법', '매뉴얼', '가이드'],
      'FAQ': ['FAQ', '자주', '질문', 'Q&A'],
      '조직 소개': ['조직', '팀', '구성원', '소개']
    };
    
    for (const [category, keywords] of Object.entries(categories)) {
      if (keywords.some(k => title.includes(k))) {
        return category;
      }
    }
    return '기타';
  }
}
```

#### 1.4 수집 API 엔드포인트 추가

```typescript
// backend/src/routes/onboarding.ts
import { Router } from 'express';
import { OnboardingCollector } from '../services/onboardingCollector';

const router = Router();

// 노션 문서 수집 API
router.post('/collect', async (req, res) => {
  try {
    const { pageIds } = req.body;
    const collector = new OnboardingCollector(pageIds);
    const documents = await collector.collectAllDocuments();
    
    res.json({
      success: true,
      count: documents.length,
      documents
    });
  } catch (error) {
    res.status(500).json({ error: 'Failed to collect documents' });
  }
});

export default router;
```

---

### 2단계: AI Agent 기반 문서 분석 및 정리

#### 2.1 문서 분석 Agent 설계

```typescript
// backend/src/services/onboardingAgent.ts
interface AnalyzedContent {
  summary: string;           // 문서 요약
  keyPoints: string[];       // 핵심 포인트
  targetAudience: string;    // 대상 독자
  difficulty: 'basic' | 'intermediate' | 'advanced';
  prerequisites: string[];   // 선행 지식
  relatedDocs: string[];     // 관련 문서
}

interface OnboardingSection {
  order: number;
  title: string;
  description: string;
  documents: {
    id: string;
    title: string;
    summary: string;
    priority: 'must-read' | 'recommended' | 'optional';
  }[];
  estimatedTime: string;
}

export class OnboardingAgent {
  
  // 문서 분석 프롬프트
  private getAnalysisPrompt(content: string): string {
    return `
당신은 글로벌 비즈니스팀의 업무 문서를 분석하는 전문가입니다.

아래 문서를 분석하여 다음 정보를 추출해주세요:

1. 요약 (2-3문장)
2. 핵심 포인트 (3-5개 불릿포인트)
3. 이 문서를 읽어야 할 대상
4. 난이도 (기초/중급/고급)
5. 선행 지식 필요 여부
6. 관련 있을 수 있는 다른 문서 주제

문서 내용:
${content}

JSON 형식으로 응답해주세요.
`;
  }
  
  // 온보딩 구조 생성 프롬프트
  private getStructurePrompt(analyzedDocs: AnalyzedContent[]): string {
    return `
당신은 신규 입사자 온보딩 프로그램을 설계하는 전문가입니다.

다음 분석된 문서들을 바탕으로 신규 입사자가 효과적으로 업무를 파악할 수 있는 
온보딩 가이드 구조를 설계해주세요.

고려사항:
- 난이도 순서 (기초 → 중급 → 고급)
- 업무 연관성 그룹핑
- 첫 주, 둘째 주, 한 달 후 등 시간대별 구분
- 필수 vs 선택 문서 구분

분석된 문서 목록:
${JSON.stringify(analyzedDocs, null, 2)}

온보딩 섹션 구조를 JSON 형식으로 응답해주세요.
`;
  }
  
  // 전체 분석 실행
  async analyzeDocuments(documents: CollectedDocument[]): Promise<AnalyzedContent[]> {
    // AI를 통한 각 문서 분석
    const analyzed: AnalyzedContent[] = [];
    
    for (const doc of documents) {
      const analysis = await this.callAI(this.getAnalysisPrompt(doc.content));
      analyzed.push(JSON.parse(analysis));
    }
    
    return analyzed;
  }
  
  // 온보딩 구조 생성
  async generateOnboardingStructure(analyzedDocs: AnalyzedContent[]): Promise<OnboardingSection[]> {
    const structure = await this.callAI(this.getStructurePrompt(analyzedDocs));
    return JSON.parse(structure);
  }
}
```

#### 2.2 문서 정리 결과 예시

```json
{
  "onboardingStructure": {
    "title": "글로벌 비즈니스팀 신규 입사자 온보딩 가이드",
    "sections": [
      {
        "order": 1,
        "title": "🏢 첫째 날: 조직과 팀 이해하기",
        "description": "팀 구조, 역할, 주요 업무 흐름을 파악합니다.",
        "documents": [
          {
            "title": "글로벌 비즈니스팀 소개",
            "summary": "팀의 미션, 비전, 조직 구성에 대한 안내",
            "priority": "must-read"
          },
          {
            "title": "팀원 소개 및 역할",
            "summary": "각 팀원의 담당 업무와 연락처",
            "priority": "must-read"
          }
        ],
        "estimatedTime": "1시간"
      },
      {
        "order": 2,
        "title": "📋 첫째 주: 기본 업무 프로세스",
        "description": "일상적인 업무 처리 방법을 학습합니다.",
        "documents": [
          {
            "title": "일일 업무 체크리스트",
            "priority": "must-read"
          },
          {
            "title": "주간 리포트 작성 가이드",
            "priority": "recommended"
          }
        ],
        "estimatedTime": "3시간"
      }
    ]
  }
}
```

---

### 3단계: 온보딩 페이지 UI/UX 설계 및 구현

#### 3.1 페이지 구조 설계

```
온보딩 메인 페이지
├── 헤더
│   ├── 환영 메시지
│   ├── 진행률 표시
│   └── 빠른 링크
│
├── 온보딩 타임라인
│   ├── Day 1 섹션
│   ├── Week 1 섹션
│   ├── Month 1 섹션
│   └── 이후 섹션
│
├── 문서 카드 그리드
│   ├── 카테고리별 필터
│   ├── 검색 기능
│   └── 읽음 표시 기능
│
├── 사이드바
│   ├── 체크리스트
│   ├── 연락처
│   └── 자주 묻는 질문
│
└── 푸터
    └── 피드백 링크
```

#### 3.2 프론트엔드 컴포넌트 구현

```tsx
// frontend/app/onboarding/page.tsx
'use client';

import { useState, useEffect } from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Progress } from '@/components/ui/progress';
import { Badge } from '@/components/ui/badge';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';

interface OnboardingDocument {
  id: string;
  title: string;
  summary: string;
  category: string;
  priority: 'must-read' | 'recommended' | 'optional';
  isRead: boolean;
  notionUrl: string;
  estimatedTime: string;
}

interface OnboardingSection {
  order: number;
  title: string;
  description: string;
  documents: OnboardingDocument[];
  estimatedTime: string;
}

export default function OnboardingPage() {
  const [sections, setSections] = useState<OnboardingSection[]>([]);
  const [progress, setProgress] = useState(0);
  const [activeTab, setActiveTab] = useState('timeline');
  
  useEffect(() => {
    fetchOnboardingData();
  }, []);
  
  const fetchOnboardingData = async () => {
    const response = await fetch('/api/onboarding/structure');
    const data = await response.json();
    setSections(data.sections);
    calculateProgress(data.sections);
  };
  
  const calculateProgress = (sections: OnboardingSection[]) => {
    const totalDocs = sections.flatMap(s => s.documents).length;
    const readDocs = sections.flatMap(s => s.documents).filter(d => d.isRead).length;
    setProgress(Math.round((readDocs / totalDocs) * 100));
  };
  
  const markAsRead = async (docId: string) => {
    await fetch(`/api/onboarding/progress/${docId}`, { method: 'POST' });
    fetchOnboardingData();
  };
  
  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900">
      <div className="container mx-auto px-4 py-8">
        {/* 헤더 섹션 */}
        <header className="mb-8">
          <h1 className="text-4xl font-bold text-white mb-2">
            🌏 글로벌 비즈니스팀 온보딩 가이드
          </h1>
          <p className="text-slate-400 text-lg">
            환영합니다! 아래 가이드를 따라 팀과 업무를 파악해보세요.
          </p>
          
          {/* 진행률 */}
          <div className="mt-6 bg-slate-800 p-4 rounded-lg">
            <div className="flex justify-between mb-2">
              <span className="text-slate-300">온보딩 진행률</span>
              <span className="text-emerald-400 font-bold">{progress}%</span>
            </div>
            <Progress value={progress} className="h-3" />
          </div>
        </header>
        
        {/* 탭 네비게이션 */}
        <Tabs value={activeTab} onValueChange={setActiveTab} className="space-y-6">
          <TabsList className="bg-slate-800">
            <TabsTrigger value="timeline">📅 타임라인</TabsTrigger>
            <TabsTrigger value="category">📁 카테고리별</TabsTrigger>
            <TabsTrigger value="checklist">✅ 체크리스트</TabsTrigger>
          </TabsList>
          
          {/* 타임라인 뷰 */}
          <TabsContent value="timeline" className="space-y-6">
            {sections.map((section) => (
              <OnboardingSection 
                key={section.order}
                section={section}
                onMarkRead={markAsRead}
              />
            ))}
          </TabsContent>
          
          {/* 카테고리별 뷰 */}
          <TabsContent value="category">
            <CategoryView sections={sections} onMarkRead={markAsRead} />
          </TabsContent>
          
          {/* 체크리스트 뷰 */}
          <TabsContent value="checklist">
            <ChecklistView sections={sections} onMarkRead={markAsRead} />
          </TabsContent>
        </Tabs>
      </div>
    </div>
  );
}

// 온보딩 섹션 컴포넌트
function OnboardingSection({ 
  section, 
  onMarkRead 
}: { 
  section: OnboardingSection; 
  onMarkRead: (id: string) => void;
}) {
  return (
    <Card className="bg-slate-800 border-slate-700">
      <CardHeader>
        <div className="flex justify-between items-center">
          <CardTitle className="text-white">{section.title}</CardTitle>
          <Badge variant="outline" className="text-slate-400">
            ⏱️ {section.estimatedTime}
          </Badge>
        </div>
        <p className="text-slate-400">{section.description}</p>
      </CardHeader>
      <CardContent>
        <div className="grid gap-4 md:grid-cols-2">
          {section.documents.map((doc) => (
            <DocumentCard 
              key={doc.id} 
              document={doc} 
              onMarkRead={onMarkRead}
            />
          ))}
        </div>
      </CardContent>
    </Card>
  );
}

// 문서 카드 컴포넌트
function DocumentCard({ 
  document, 
  onMarkRead 
}: { 
  document: OnboardingDocument; 
  onMarkRead: (id: string) => void;
}) {
  const priorityColors = {
    'must-read': 'bg-red-500',
    'recommended': 'bg-yellow-500',
    'optional': 'bg-blue-500'
  };
  
  const priorityLabels = {
    'must-read': '필수',
    'recommended': '권장',
    'optional': '선택'
  };
  
  return (
    <div className={`
      p-4 rounded-lg border transition-all duration-200
      ${document.isRead 
        ? 'bg-slate-700/50 border-emerald-500/50' 
        : 'bg-slate-700 border-slate-600 hover:border-slate-500'}
    `}>
      <div className="flex justify-between items-start mb-2">
        <h4 className="font-medium text-white">{document.title}</h4>
        <Badge className={priorityColors[document.priority]}>
          {priorityLabels[document.priority]}
        </Badge>
      </div>
      <p className="text-slate-400 text-sm mb-3">{document.summary}</p>
      <div className="flex justify-between items-center">
        <a 
          href={document.notionUrl} 
          target="_blank"
          className="text-emerald-400 hover:text-emerald-300 text-sm"
        >
          📖 문서 보기 →
        </a>
        <button 
          onClick={() => onMarkRead(document.id)}
          className={`text-sm px-3 py-1 rounded ${
            document.isRead 
              ? 'bg-emerald-500/20 text-emerald-400' 
              : 'bg-slate-600 text-slate-300 hover:bg-slate-500'
          }`}
        >
          {document.isRead ? '✓ 완료' : '읽음 표시'}
        </button>
      </div>
    </div>
  );
}
```

#### 3.3 백엔드 API 구현

```typescript
// backend/src/routes/onboarding.ts (확장)

// 온보딩 구조 조회
router.get('/structure', async (req, res) => {
  try {
    const userId = req.user?.id;
    const structure = await onboardingService.getStructure();
    const progress = await onboardingService.getUserProgress(userId);
    
    // 사용자 진행률 병합
    const mergedStructure = mergeProgressWithStructure(structure, progress);
    
    res.json(mergedStructure);
  } catch (error) {
    res.status(500).json({ error: 'Failed to get onboarding structure' });
  }
});

// 진행률 업데이트
router.post('/progress/:documentId', async (req, res) => {
  try {
    const { documentId } = req.params;
    const userId = req.user?.id;
    
    await onboardingService.markAsRead(userId, documentId);
    
    res.json({ success: true });
  } catch (error) {
    res.status(500).json({ error: 'Failed to update progress' });
  }
});

// 노션에서 최신 문서 동기화
router.post('/sync', async (req, res) => {
  try {
    const result = await onboardingService.syncFromNotion();
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: 'Failed to sync from Notion' });
  }
});
```

---

### 4단계: 배포 및 자동화 설정

#### 4.1 노션 동기화 자동화

```typescript
// backend/src/jobs/notionSyncJob.ts
import cron from 'node-cron';
import { OnboardingService } from '../services/onboardingService';

// 매일 오전 9시 자동 동기화
export function setupNotionSyncJob() {
  cron.schedule('0 9 * * *', async () => {
    console.log('[Notion Sync] Starting daily sync...');
    
    try {
      const onboardingService = new OnboardingService();
      const result = await onboardingService.syncFromNotion();
      
      console.log(`[Notion Sync] Completed. Updated: ${result.updated}, New: ${result.new}`);
    } catch (error) {
      console.error('[Notion Sync] Failed:', error);
    }
  });
  
  console.log('[Notion Sync] Daily sync job scheduled for 9:00 AM');
}
```

#### 4.2 변경 감지 및 알림

```typescript
// backend/src/services/notificationService.ts
export class NotificationService {
  
  // 문서 업데이트 알림
  async notifyDocumentUpdate(updatedDocs: string[]) {
    // Slack 웹훅으로 알림
    await fetch(process.env.SLACK_WEBHOOK_URL, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        text: `📝 온보딩 문서가 업데이트되었습니다!\n업데이트된 문서: ${updatedDocs.join(', ')}`
      })
    });
  }
}
```

---

## 🔧 실행 체크리스트

### Phase 1: 환경 설정 (1일)

- [ ] Notion API 키 확인 및 설정
- [ ] 학습할 노션 페이지 목록 확정
- [ ] 각 페이지에 통합 공유 설정
- [ ] 백엔드 환경변수 업데이트

### Phase 2: 문서 수집 및 분석 (2-3일)

- [ ] 노션 페이지 ID 수집
- [ ] 문서 수집 스크립트 실행
- [ ] AI Agent로 문서 분석
- [ ] 온보딩 구조 생성 및 검토

### Phase 3: 프론트엔드 구현 (3-5일)

- [ ] 온보딩 페이지 컴포넌트 구현
- [ ] 진행률 추적 기능 구현
- [ ] 검색 및 필터 기능 구현
- [ ] 반응형 디자인 적용

### Phase 4: 통합 및 테스트 (2일)

- [ ] 백엔드 API 연동 테스트
- [ ] 사용자 시나리오 테스트
- [ ] 성능 최적화
- [ ] 버그 수정

### Phase 5: 배포 및 운영 (1일)

- [ ] 스테이징 환경 배포
- [ ] 자동 동기화 작업 설정
- [ ] 모니터링 설정
- [ ] 프로덕션 배포

---

## 📊 예상 결과물

### 완성된 온보딩 페이지 기능

1. **타임라인 뷰**: 입사 후 시간대별 학습 순서 안내
2. **카테고리 뷰**: 주제별 문서 그룹핑
3. **체크리스트**: 진행 상황 추적 및 완료 표시
4. **검색 기능**: 키워드 기반 문서 검색
5. **진행률 대시보드**: 전체 온보딩 진행 현황
6. **노션 연동**: 원본 문서로 바로 이동

### 자동화된 기능

1. **일일 동기화**: 노션 문서 변경사항 자동 반영
2. **변경 알림**: 문서 업데이트 시 Slack 알림
3. **진행률 저장**: 사용자별 학습 진행률 저장

---

## 🎯 다음 단계

이 가이드를 기반으로 실제 구현을 시작하려면:

1. **노션 페이지 목록 제공**: 학습할 노션 페이지 URL/ID 목록
2. **카테고리 기준 확정**: 문서 분류 기준 결정
3. **디자인 참고자료**: 원하는 UI/UX 스타일 참고

위 정보를 제공해주시면 바로 구현을 시작할 수 있습니다!

---

## 📝 참고 문서

- [Notion API 연동 가이드](./NOTION_INTEGRATION.md)
- [Agent 설계 문서](../agent_design.md)
- [프론트엔드 README](../frontend/README.md)

