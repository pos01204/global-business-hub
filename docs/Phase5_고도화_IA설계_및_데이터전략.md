# 🚀 Phase 5 고도화: IA 설계 및 데이터 활용 전략

> **문서 버전**: 1.0  
> **작성일**: 2024-12-17  
> **범위**: IA/메뉴 구조 설계 + 데이터 활용 고도화  
> **제외 범위**: ⛔ 캐싱 및 성능 최적화 (별도 문서로 분리)  
> **참조 문서**: `데이터_규칙_가이드.md`, `허브_개선_제안서.md`

---

## 📋 목차

1. [Executive Summary](#1-executive-summary)
2. [현재 허브 메뉴 구조 (As-Is)](#2-현재-허브-메뉴-구조-as-is)
3. [Phase 5 신규 페이지 IA 설계](#3-phase-5-신규-페이지-ia-설계)
4. [데이터 활용 현황 진단 (As-Is)](#4-데이터-활용-현황-진단-as-is)
5. [데이터 스케일업 전략 (To-Be)](#5-데이터-스케일업-전략-to-be)
6. [2차 가공 데이터 설계](#6-2차-가공-데이터-설계)
7. [구현 로드맵](#7-구현-로드맵)
8. [부록: 데이터 흐름도](#8-부록-데이터-흐름도)

---

## 1. Executive Summary

### 🎯 Phase 5 목표

| 목표 | 현재 | To-Be | 핵심 성과 |
|------|:----:|:-----:|----------|
| **데이터 활용 레벨** | 1차 단순 조회 | 2차+ 복합 분석 | 의사결정 지원 가능 |
| **메뉴 구조 효율성** | 단순 나열 | 목적 기반 그룹핑 | UX 향상 |
| **데이터 재사용률** | ~20% | ~80% | 중복 제거 |
| **분석 자동화 수준** | 수동 해석 | 자동 인사이트 | 운영 효율화 |

### 📊 Phase 5 핵심 산출물

| # | 산출물 | 유형 | 우선순위 |
|:-:|--------|------|:--------:|
| 1 | **쿠폰 효과 분석** | 신규 페이지 | P0 |
| 2 | **리뷰 분석 대시보드** | 신규 페이지 | P0 |
| 3 | **통합 고객 360° 뷰** | 신규 페이지 | P1 |
| 4 | **주문 패턴 분석** | 신규 페이지 | P2 |
| 5 | **2차 가공 데이터 레이어** | 데이터 인프라 | P0 |
| 6 | **분석 IA 재구조화** | 메뉴 구조 | P1 |

---

## 2. 현재 허브 메뉴 구조 (As-Is)

### 2.1 메뉴 트리

```
idus Global Hub
├── 🏠 홈
│   └── 대시보드
│
├── 📦 물류 운영
│   ├── 미입고 관리
│   ├── 분류 추적
│   └── 물류 관제 센터
│
├── 💰 분류비 정산
│
├── 🔧 업무 지원
│   ├── QC 관리
│   ├── 소포수령증
│   └── 통합 검색
│
├── 📊 분석
│   ├── 성과 분석
│   ├── 고객 분석
│   ├── 작가 분석
│   └── 비용 & 손익
│
├── 🧠 경영 인사이트
│   └── Business Brain
│
├── 💬 고객 인사이트
│   └── 고객 리뷰
│
└── 🛠️ 도구
    ├── 피포먼스 마케팅
    ├── 쿠폰 생성/발급
    └── AI 어시스턴트
```

### 2.2 현재 구조의 한계

| # | 문제점 | 상세 설명 | 영향 |
|:-:|--------|----------|------|
| 1 | **분석 기능 분산** | 성과/고객/작가 분석이 개별 메뉴로 분리되어 교차 분석 어려움 | UX 저하 |
| 2 | **인사이트 단절** | Business Brain ↔ 고객 리뷰 간 연계 부재 | 분석 단절 |
| 3 | **도구-분석 혼재** | 쿠폰 생성은 '도구'지만 쿠폰 분석 기능 부재 | 기능 누락 |
| 4 | **데이터 재사용 불가** | 각 페이지가 독립적으로 데이터 조회 → 중복 연산 | 비효율 |

---

## 3. Phase 5 신규 페이지 IA 설계

### 3.1 신규 페이지 목록

| 페이지 | 목적 | 주요 데이터 소스 |
|--------|------|-----------------|
| **쿠폰 효과 분석** | 마케팅 ROI 측정 | `coupon` + `order` |
| **리뷰 분석 대시보드** | 고객 만족도 심층 분석 | `review` |
| **통합 고객 360° 뷰** | 개별 고객 전체 여정 조회 | `users` + `order` + `review` + `coupon` |
| **주문 패턴 분석** | 시간/채널/계절성 분석 | `order` (파생 컬럼) |

### 3.2 메뉴 배치 전략 비교

#### 🅰️ 안 A: 기존 메뉴 하위 편입

```
📊 분석 (기존)
├── 성과 분석
├── 고객 분석
│   └── 🆕 고객 360° 뷰  ← 하위 메뉴
├── 작가 분석
├── 비용 & 손익
├── 🆕 주문 패턴 분석     ← 신규 추가
└── 🆕 쿠폰 효과 분석     ← 신규 추가

💬 고객 인사이트 (기존)
├── 고객 리뷰
└── 🆕 리뷰 분석 대시보드  ← 신규 추가
```

| 장점 | 단점 |
|------|------|
| ✅ 기존 구조 유지 | ❌ 분석 메뉴 과부하 (7개 항목) |
| ✅ 사용자 학습 비용 최소화 | ❌ 쿠폰 분석 ↔ 쿠폰 도구 위치 분리 |
| ✅ 즉시 적용 가능 | ❌ 교차 분석 흐름 개선 없음 |

**사용자 관점**: 기존 메뉴 위치 기억 활용 가능, 단 메뉴 깊이 증가로 탐색 시간 ↑

---

#### 🅱️ 안 B: 중간 허브 확장 (⭐ **권장안**)

```
📊 분석 (기존 → 확장)
├── 📈 성과 분석 허브
│   ├── 성과 분석 (기존)
│   ├── 🆕 주문 패턴 분석
│   └── 🆕 쿠폰 효과 분석
│
├── 👥 고객 분석 허브
│   ├── 고객 분석 (기존)
│   ├── 🆕 고객 360° 뷰
│   └── 🆕 리뷰 분석
│
├── 🎨 작가 분석 (기존)
└── 💵 비용 & 손익 (기존)
```

| 장점 | 단점 |
|------|------|
| ✅ **목적 기반 그룹핑** (성과 vs 고객) | ⚠️ 메뉴 재구조화 작업 필요 |
| ✅ **확장성 확보** - 각 허브에 기능 추가 용이 | ⚠️ 사용자 재학습 필요 |
| ✅ **관련 분석 집약** - 교차 분석 흐름 개선 | |
| ✅ **2차 가공 데이터 공유** 구조에 적합 | |

**사용자 관점**:
- **운영팀**: 성과 허브에서 주문→쿠폰 효과까지 한 흐름으로 분석
- **기획팀**: 고객 허브에서 세그먼트→360°뷰→리뷰까지 연계 분석
- **실무자**: 명확한 목적별 메뉴로 탐색 효율 ↑

**확장 이점**:
- 성과 허브: 향후 채널별 분석, A/B 테스트 결과 등 추가 가능
- 고객 허브: LTV 예측, 이탈 예측, 코호트 분석 등 추가 가능

---

#### 🅲️ 안 C: 완전 독립 신규 허브

```
📊 분석 (기존 유지)

🔬 고급 분석 (완전 신규)
├── 🆕 쿠폰 효과 분석
├── 🆕 리뷰 분석 대시보드
├── 🆕 고객 360° 뷰
├── 🆕 주문 패턴 분석
└── 🆕 예측 모델링 (향후)
```

| 장점 | 단점 |
|------|------|
| ✅ 기존 메뉴 완전 보존 | ❌ **메뉴 과다화** (IA 복잡도 ↑) |
| ✅ 고급 기능 명확한 분리 | ❌ 기존 분석과 중복 인식 |
| ✅ 권한 분리 용이 | ❌ 데이터 재사용 구조와 부합하지 않음 |

**사용자 관점**: 고급 사용자 전용 공간이나, 기존 분석과의 관계 모호

---

### 3.3 권장안 상세: 🅱️ 중간 허브 확장

#### 최종 메뉴 구조

```
idus Global Hub
│
├── 🏠 홈
│   └── 대시보드
│
├── 📦 물류 운영
│   ├── 미입고 관리
│   ├── 분류 추적
│   └── 물류 관제 센터
│
├── 💰 분류비 정산
│
├── 🔧 업무 지원
│   ├── QC 관리
│   ├── 소포수령증
│   └── 통합 검색
│
├── 📊 분석                        ← 확장
│   │
│   ├── 📈 성과 분석 허브           ← 신규 허브
│   │   ├── 성과 분석 (기존)
│   │   ├── 🆕 주문 패턴 분석
│   │   └── 🆕 쿠폰 효과 분석
│   │
│   ├── 👥 고객 분석 허브           ← 신규 허브
│   │   ├── 고객 분석 (기존)
│   │   ├── 🆕 고객 360° 뷰
│   │   └── 🆕 리뷰 분석
│   │
│   ├── 🎨 작가 분석
│   └── 💵 비용 & 손익
│
├── 🧠 경영 인사이트
│   └── Business Brain
│
└── 🛠️ 도구
    ├── 피포먼스 마케팅
    ├── 쿠폰 생성/발급             ← 생성/운영 도구
    └── AI 어시스턴트
```

#### 사용자별 이용 흐름

| 사용자 | 주요 시나리오 | 이용 흐름 |
|--------|-------------|----------|
| **운영팀** | 일일 성과 점검 | 대시보드 → 성과 허브 → 주문 패턴 |
| **마케팅팀** | 캠페인 효과 분석 | 성과 허브 → 쿠폰 효과 분석 |
| **기획팀** | 고객 인사이트 도출 | 고객 허브 → 360° 뷰 → 리뷰 분석 |
| **경영진** | 전략 의사결정 | Business Brain → 성과 허브 요약 |
| **CS팀** | 개별 고객 대응 | 고객 허브 → 360° 뷰 (검색) |

---

## 4. 데이터 활용 현황 진단 (As-Is)

### 4.1 데이터 활용 레벨 정의

```
┌─────────────────────────────────────────────────────────────────┐
│                    데이터 활용 성숙도 모델                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Level 4: 예측/자동화  ─── 예측 모델, 자동 추천, 시나리오 분석    │
│       ▲                                                         │
│  Level 3: 원인 분석    ─── Why 분석, 상관관계, 세그먼트 비교      │
│       ▲                                                         │
│  Level 2: 트렌드/비교  ─── 기간 비교, 추이 분석, 집계 통계        │
│       ▲                                                         │
│  Level 1: 단순 조회    ─── Raw 데이터 표시, 단발성 집계           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 페이지별 현재 활용 레벨

| 페이지 | 현재 레벨 | 목표 레벨 | 주요 한계 |
|--------|:--------:|:--------:|----------|
| **대시보드** | L2 | L3 | 트렌드만 표시, 원인 분석 불가 |
| **성과 분석** | L2 | L3 | 기간 비교 가능, 세그먼트 드릴다운 부족 |
| **고객 분석** | L2 | L3 | RFM 있으나 행동 원인 분석 부재 |
| **작가 분석** | L2 | L3 | 순위만 표시, 성장/하락 원인 미분석 |
| **Business Brain** | L3 | L4 | 인사이트 제공, 예측 모델 미흡 |
| **고객 리뷰** | **L1** | L3 | ⚠️ **단순 조회만**, 분석 기능 전무 |
| **쿠폰 도구** | **L0** | L2 | ⚠️ **생성만**, 효과 분석 없음 |

### 4.3 데이터 활용 한계 상세 분석

#### 🔴 문제 1: 단순 조회/단발성 분석에 머무르는 영역

| 영역 | 현재 상태 | 한계 |
|------|----------|------|
| **고객 리뷰** | 리뷰 목록 나열, 별점 표시 | NPS 미계산, 트렌드 미분석, 키워드 미추출 |
| **쿠폰** | 생성/발급 기능만 존재 | 사용률, ROI, 효과 분석 전무 |
| **주문 시간 정보** | 주문일만 활용 | 요일별, 시간대별 패턴 미분석 |
| **국가별 데이터** | 단순 합계만 | 국가별 고객 행동 차이 미분석 |

#### 🔴 문제 2: Raw Data 위주 활용의 한계

| 데이터 | 현재 활용 | 가공 부재로 인한 한계 |
|--------|----------|---------------------|
| `order.order_date` | 일별 집계 | 요일/시즌 패턴 파생 안됨 |
| `review.rating` | 평균만 계산 | NPS 세그먼트 분류 안됨 |
| `coupon.used_count` | 미사용 | 전환율 = used/issued 미계산 |
| `users.first_order_date` | 미사용 | 코호트 분석 불가 |

#### 🔴 문제 3: 가공/누적/비교 분석이 어려운 구조적 문제

```
현재 데이터 흐름:

[Google Sheets] ──(매 요청)──> [Backend] ──(매번 재계산)──> [Frontend]
                                   │
                                   ▼
                           • 동일 집계 반복 수행
                           • 페이지 간 데이터 공유 없음
                           • 히스토리 데이터 아카이빙 없음
                           • 파생 지표 저장 안됨
```

| 구조적 문제 | 영향 |
|------------|------|
| **사전 집계 없음** | 매번 raw 데이터에서 집계 → 응답 지연 |
| **파생 지표 미저장** | 동일 계산 반복 (예: 전환율, NPS) |
| **히스토리 아카이빙 없음** | 과거 시점 스냅샷 비교 불가 |
| **페이지 간 데이터 격리** | 동일 고객 정보를 각 페이지가 독립 조회 |

---

## 5. 데이터 스케일업 전략 (To-Be)

### 5.1 데이터 가공 단계 정의

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         데이터 가공 레이어 아키텍처                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌───────────┐ │
│  │   Raw Data  │ ──► │  1차 가공   │ ──► │  2차 가공   │ ──► │  n차 가공 │ │
│  │  (원천)     │     │  (정제)     │     │  (분석용)   │     │  (예측)   │ │
│  └─────────────┘     └─────────────┘     └─────────────┘     └───────────┘ │
│        │                   │                   │                   │       │
│        ▼                   ▼                   ▼                   ▼       │
│   Google Sheets       표준화/분류          지표 결합            ML 모델    │
│   (order, review,     NULL 처리            기간 비교            예측 점수  │
│    coupon, ...)       타입 변환            파생 지표            추천 결과  │
│                       기본 집계            누적 추이                       │
│                                            세그먼트 집계                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 각 레이어 상세 정의

#### 📦 Layer 0: Raw Data (원천 데이터)

| 시트 | 컬럼 수 | 갱신 주기 | 저장 위치 |
|------|:------:|:--------:|----------|
| `order` | 102 | 실시간 | Google Sheets |
| `review` | 20 | 실시간 | Google Sheets |
| `coupon` | 22 | 실시간 | Google Sheets |
| `users` | 10 | 실시간 | Google Sheets |
| `logistics` | 35 | 실시간 | Google Sheets |
| `artists` | 25 | 실시간 | Google Sheets |

---

#### 🔧 Layer 1: 1차 가공 데이터 (정제/표준화)

**목적**: Raw 데이터의 정제, 표준화, 기본 집계, 분류 기준 적용

| 가공 유형 | 예시 | 저장 여부 |
|----------|------|:--------:|
| **NULL 처리** | `null` → 0, 빈 문자열 → 'N/A' | 인메모리 |
| **타입 표준화** | 날짜 → `Date`, 금액 → `number` | 인메모리 |
| **환율 변환** | USD → KRW (1,350원 고정) | 인메모리 |
| **기본 분류** | 국가 코드 → 국가명 | 인메모리 |
| **일별 집계** | 일별 GMV, 주문 수, 신규 고객 | ✅ **저장** |

**1차 가공 데이터 명세**:

```typescript
// 1차 가공: 일별 기본 집계
interface DailyBasicMetrics {
  date: string                    // YYYY-MM-DD
  
  // 주문 기본 지표
  orderCount: number              // 일 주문 수
  totalGMV: number               // 일 GMV (KRW)
  totalGMV_USD: number           // 일 GMV (USD, 원본)
  avgAOV: number                 // 평균 주문 금액
  
  // 고객 기본 지표
  uniqueCustomers: number        // 고유 고객 수
  newCustomers: number           // 신규 고객 수
  returningCustomers: number     // 재구매 고객 수
  
  // 상품/작가 기본 지표
  uniqueArtists: number          // 고유 작가 수
  uniqueProducts: number         // 고유 상품 수
  
  // 국가별 분리
  byCountry: {
    JP: { orderCount: number; gmv: number }
    EN: { orderCount: number; gmv: number }
  }
}

// 1차 가공: 리뷰 일별 집계
interface DailyReviewMetrics {
  date: string
  reviewCount: number
  avgRating: number
  ratingDistribution: { [key: number]: number }  // 1~5점별 개수
}

// 1차 가공: 쿠폰 일별 집계
interface DailyCouponMetrics {
  date: string
  issuedCount: number
  usedCount: number
  totalDiscountAmount: number
  totalGMVFromCoupon: number
}
```

**저장 전략**:
- **일별 집계**: DB/캐시에 영구 저장 (아카이빙)
- **실시간 처리**: 인메모리 변환 후 응답

---

#### 📊 Layer 2: 2차 가공 데이터 (분석용)

**목적**: 지표 간 결합, 기간/집단 비교, 파생 지표 생성, 누적·추이 분석

| 가공 유형 | 예시 | 활용 목적 |
|----------|------|----------|
| **지표 결합** | 전환율 = used_count / issue_count | 단일 지표로 효과 측정 |
| **기간 비교** | 전주 대비 GMV 변화율 | 트렌드 파악 |
| **세그먼트 집계** | RFM별 고객 수, 평균 구매액 | 그룹별 특성 파악 |
| **파생 지표** | NPS = Promoters% - Detractors% | 복합 지표 생성 |
| **누적 추이** | MAU 30일 이동평균 | 노이즈 제거된 트렌드 |

**2차 가공 데이터 명세**:

```typescript
// 2차 가공: 쿠폰 분석 지표
interface CouponAnalyticsMetrics {
  period: { start: string; end: string }
  
  // 파생 지표 (1차 데이터 결합)
  conversionRate: number          // used_count / issue_count
  roi: number                     // gmv / discount_amount
  avgDiscountPerOrder: number     // discount_amount / used_count
  
  // 기간 비교
  conversionRateChange: number    // vs 이전 기간
  roiChange: number               // vs 이전 기간
  
  // 세그먼트별 집계
  byDiscountType: {
    RATE: CouponSegmentMetrics
    FIXED: CouponSegmentMetrics
  }
  byCouponType: {
    idus: CouponSegmentMetrics
    CRM: CouponSegmentMetrics
    Sodam: CouponSegmentMetrics
  }
  byCountry: {
    JP: CouponSegmentMetrics
    EN: CouponSegmentMetrics
  }
  
  // 랭킹 (정렬된 파생 데이터)
  topByConversion: CouponRankItem[]
  topByROI: CouponRankItem[]
  failedCoupons: CouponRankItem[]  // 전환율 0%
}

// 2차 가공: 고객 분석 지표
interface CustomerAnalyticsMetrics {
  period: { start: string; end: string }
  
  // NPS (파생 지표)
  nps: number                     // Promoters% - Detractors%
  promoterCount: number           // 5점 리뷰 고객
  passiveCount: number            // 4점 리뷰 고객
  detractorCount: number          // 1~3점 리뷰 고객
  
  // RFM 세그먼트별 집계
  rfmSegments: {
    champions: CustomerSegment
    loyalCustomers: CustomerSegment
    potentialLoyalist: CustomerSegment
    atRisk: CustomerSegment
    cantLoseThem: CustomerSegment
    hibernating: CustomerSegment
  }
  
  // 코호트 분석 (가입월별)
  cohortRetention: {
    [cohortMonth: string]: {
      month0: number  // 가입월
      month1: number  // +1개월 잔존율
      month2: number  // +2개월 잔존율
      // ...
    }
  }
  
  // 기간 비교
  npsChange: number
  avgRatingChange: number
}

// 2차 가공: 성과 분석 지표
interface PerformanceAnalyticsMetrics {
  period: { start: string; end: string }
  
  // 기간 비교 지표
  gmvGrowthRate: number           // vs 이전 기간
  orderGrowthRate: number
  aovGrowthRate: number
  
  // 트렌드 (이동평균)
  gmvTrend: TrendPoint[]          // 7일 이동평균
  orderTrend: TrendPoint[]
  
  // 요일별 패턴 (2차 집계)
  dayOfWeekPattern: {
    [day: string]: { avgOrders: number; avgGMV: number; peakHour: number }
  }
  
  // 시즈널리티 (분기별)
  quarterlyPattern: {
    Q1: { avgGMV: number; growthVsPrevYear: number }
    Q2: { avgGMV: number; growthVsPrevYear: number }
    Q3: { avgGMV: number; growthVsPrevYear: number }
    Q4: { avgGMV: number; growthVsPrevYear: number }
  }
}
```

**저장 전략**:
- **주기적 배치 저장**: 매일 자정 집계 후 DB 저장
- **온디맨드 계산**: 사용자 요청 시 계산 (캐시 활용)

---

#### 🔮 Layer 3+: n차 가공 데이터 (예측/자동화)

**목적**: 예측 모델, 추천, 시나리오 분석, 데이터 자산화

| 가공 유형 | 예시 | 활용 목적 |
|----------|------|----------|
| **예측 모델** | 다음 달 GMV 예측 | 목표 설정 |
| **이탈 예측** | 고객별 이탈 확률 점수 | 선제 대응 |
| **추천** | 최적 쿠폰 타입 추천 | 마케팅 자동화 |
| **시나리오 분석** | "쿠폰 할인율 5% 증가 시" | What-if 분석 |

```typescript
// 3차 가공: 예측 모델 출력
interface PredictionMetrics {
  // GMV 예측
  gmvForecast: {
    nextWeek: { predicted: number; confidence: number }
    nextMonth: { predicted: number; confidence: number }
  }
  
  // 이탈 예측
  churnRiskCustomers: Array<{
    userId: string
    churnProbability: number
    lastOrderDate: string
    recommendedAction: string
  }>
  
  // 쿠폰 추천
  couponRecommendations: Array<{
    targetSegment: string
    recommendedType: 'RATE' | 'FIXED'
    recommendedDiscount: number
    expectedConversion: number
    expectedROI: number
  }>
}
```

**현재 Phase 5 범위**: Layer 3는 구조만 설계, 실제 구현은 향후 Phase

---

### 5.3 2차 가공 데이터 활용 목적

#### 단순 현황 파악을 넘어서는 분석

| 분석 유형 | 기존 (L1) | To-Be (L2+) |
|----------|----------|-------------|
| **What** (무엇이) | "이번 달 GMV는 1.2억" | ✅ 제공 중 |
| **When** (언제) | "3월에 피크" | ✅ 제공 중 |
| **Why** (왜) | ❌ 미제공 | "CRM 쿠폰 캠페인 효과로 재구매율 15% ↑" |
| **Who** (누가) | ❌ 미제공 | "Champions 세그먼트가 전체 GMV의 45% 기여" |
| **How** (어떻게) | ❌ 미제공 | "FIXED 쿠폰이 RATE 대비 전환율 2.3배 높음" |

#### 세부 활용 시나리오

| 시나리오 | 필요 데이터 | 활용 목적 |
|----------|-----------|----------|
| 쿠폰 최적화 | 쿠폰 유형별 전환율, ROI | "FIXED 10% 쿠폰이 ROI 최고" |
| 고객 이탈 방지 | RFM 점수 추이, 마지막 구매일 | "At Risk 세그먼트에 재구매 유도" |
| 작가 성장 지원 | 작가별 GMV 추이, 리뷰 평점 | "평점 하락 작가 품질 개선 안내" |
| 계절 마케팅 | 요일/분기별 주문 패턴 | "금요일 피크 시간대 프로모션" |

---

### 5.4 데이터 구조 설계 관점

#### 아카이빙 vs 재생산 기준

| 데이터 유형 | 처리 방식 | 이유 |
|------------|----------|------|
| **일별 집계** (1차) | ✅ 아카이빙 | 과거 데이터 재계산 비용 높음 |
| **NPS 점수** (2차) | ✅ 아카이빙 | 시점별 비교 필요 |
| **RFM 점수** (2차) | ✅ 아카이빙 | 고객 여정 추적 필요 |
| **기간 비교 지표** | 온디맨드 재생산 | 비교 기간이 가변적 |
| **랭킹 데이터** | 온디맨드 재생산 | 실시간 최신 상태 필요 |

#### 페이지 간 데이터 재사용

```
                         ┌──────────────────────────────────────┐
                         │    2차 가공 데이터 레이어 (공유)      │
                         ├──────────────────────────────────────┤
                         │  • CouponAnalyticsMetrics            │
                         │  • CustomerAnalyticsMetrics          │
                         │  • PerformanceAnalyticsMetrics       │
                         └──────────┬───────────────────────────┘
                                    │
          ┌─────────────────────────┼─────────────────────────┐
          │                         │                         │
          ▼                         ▼                         ▼
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│ 성과 분석 허브   │      │  고객 분석 허브  │      │ Business Brain  │
├─────────────────┤      ├─────────────────┤      ├─────────────────┤
│ • 성과 분석     │      │ • 고객 분석     │      │ • 종합 인사이트 │
│ • 주문 패턴    │      │ • 고객 360°    │      │ • What-if      │
│ • 쿠폰 효과    │      │ • 리뷰 분석    │      │                 │
└─────────────────┘      └─────────────────┘      └─────────────────┘
```

#### 확장 가능성 설계

| 현재 범위 | 향후 확장 | 데이터 기반 |
|----------|----------|------------|
| 쿠폰 효과 분석 | A/B 테스트 결과 저장/비교 | `CouponAnalyticsMetrics` |
| 고객 360° 뷰 | LTV 예측 모델 | `CustomerAnalyticsMetrics` |
| 주문 패턴 분석 | 자동 마케팅 트리거 | `PerformanceAnalyticsMetrics` |
| 리뷰 분석 | NLP 키워드 자동 추출 | `DailyReviewMetrics` |

---

## 6. 2차 가공 데이터 설계

### 6.1 핵심 2차 가공 데이터 엔티티

#### Entity 1: `ProcessedCouponAnalytics`

```typescript
/**
 * 쿠폰 분석용 2차 가공 데이터
 * - 생성 주기: 일 1회 (자정 배치) + 온디맨드
 * - 저장 위치: DB (aggregated_coupon_analytics 테이블)
 * - 재사용: 쿠폰 효과 분석, Dashboard, Business Brain
 */
interface ProcessedCouponAnalytics {
  // 메타데이터
  id: string
  createdAt: Date
  periodStart: Date
  periodEnd: Date
  
  // === 1차 → 2차 파생 지표 ===
  
  // 전환율 (used / issued)
  overallConversionRate: number
  conversionRateByType: {
    RATE: number
    FIXED: number
  }
  conversionRateByCountry: {
    JP: number
    EN: number
  }
  
  // ROI (gmv / discount_amount)
  overallROI: number
  roiByType: {
    RATE: number
    FIXED: number
  }
  
  // === 기간 비교 (2차 가공) ===
  conversionRateChange: number    // vs 직전 동일 기간
  roiChange: number
  
  // === 세그먼트 집계 (2차 가공) ===
  topPerformers: Array<{
    couponId: number
    couponName: string
    conversionRate: number
    roi: number
    totalGMV: number
  }>
  
  underPerformers: Array<{
    couponId: number
    couponName: string
    issueCount: number
    usedCount: number
    failureReason: string  // 자동 추론
  }>
  
  // === 인사이트 (자동 생성) ===
  insights: Array<{
    type: 'success' | 'warning' | 'info'
    message: string
    actionRecommendation: string
  }>
}
```

#### Entity 2: `ProcessedCustomerAnalytics`

```typescript
/**
 * 고객 분석용 2차 가공 데이터
 * - 생성 주기: 일 1회 (자정 배치)
 * - 저장 위치: DB (aggregated_customer_analytics 테이블)
 * - 재사용: 고객 분석, 고객 360°, Business Brain
 */
interface ProcessedCustomerAnalytics {
  id: string
  createdAt: Date
  periodStart: Date
  periodEnd: Date
  
  // === NPS (리뷰 데이터 2차 가공) ===
  nps: number
  npsDistribution: {
    promoters: number      // 5점 (%)
    passives: number       // 4점 (%)
    detractors: number     // 1~3점 (%)
  }
  npsTrend: Array<{ date: string; nps: number }>  // 일별 추이
  
  // === RFM 세그먼트 집계 ===
  rfmSummary: {
    champions: { count: number; avgLTV: number; avgOrderFreq: number }
    loyalCustomers: { count: number; avgLTV: number; avgOrderFreq: number }
    potentialLoyalist: { count: number; avgLTV: number; avgOrderFreq: number }
    newCustomers: { count: number; avgLTV: number; avgOrderFreq: number }
    promising: { count: number; avgLTV: number; avgOrderFreq: number }
    needsAttention: { count: number; avgLTV: number; avgOrderFreq: number }
    aboutToSleep: { count: number; avgLTV: number; avgOrderFreq: number }
    atRisk: { count: number; avgLTV: number; avgOrderFreq: number }
    cantLoseThem: { count: number; avgLTV: number; avgOrderFreq: number }
    hibernating: { count: number; avgLTV: number; avgOrderFreq: number }
    lost: { count: number; avgLTV: number; avgOrderFreq: number }
  }
  
  // === 코호트 리텐션 (월별 가입 기준) ===
  cohortRetention: {
    [cohortMonth: string]: number[]  // [M0, M1, M2, ...] 잔존율
  }
  
  // === 기간 비교 ===
  npsChange: number
  segmentMigration: {
    upgrades: number   // 상위 세그먼트로 이동한 고객 수
    downgrades: number // 하위 세그먼트로 이동한 고객 수
  }
}
```

#### Entity 3: `ProcessedOrderPatterns`

```typescript
/**
 * 주문 패턴 분석용 2차 가공 데이터
 * - 생성 주기: 일 1회 (자정 배치)
 * - 저장 위치: DB (aggregated_order_patterns 테이블)
 * - 재사용: 주문 패턴 분석, 성과 분석, Business Brain
 */
interface ProcessedOrderPatterns {
  id: string
  createdAt: Date
  periodStart: Date
  periodEnd: Date
  
  // === 요일별 패턴 ===
  dayOfWeekPattern: {
    monday: DayPattern
    tuesday: DayPattern
    wednesday: DayPattern
    thursday: DayPattern
    friday: DayPattern
    saturday: DayPattern
    sunday: DayPattern
  }
  peakDay: string              // 가장 주문 많은 요일
  
  // === 분기별 시즈널리티 ===
  quarterlyPattern: {
    Q1: QuarterPattern
    Q2: QuarterPattern
    Q3: QuarterPattern
    Q4: QuarterPattern
  }
  peakQuarter: string
  
  // === 트렌드 (이동평균) ===
  gmvTrend7d: TrendPoint[]     // 7일 이동평균
  gmvTrend30d: TrendPoint[]    // 30일 이동평균
  
  // === 국가별 패턴 차이 ===
  countryPatternDiff: {
    JP: { peakDay: string; peakQuarter: string; avgOrderTime: string }
    EN: { peakDay: string; peakQuarter: string; avgOrderTime: string }
  }
}

interface DayPattern {
  avgOrders: number
  avgGMV: number
  orderShare: number  // 전체 대비 %
}

interface QuarterPattern {
  totalGMV: number
  avgDailyGMV: number
  growthVsPrevQuarter: number
  growthVsPrevYear: number
}
```

### 6.2 데이터 흐름 및 저장 전략

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            데이터 처리 파이프라인                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   [Google Sheets]                                                           │
│        │                                                                    │
│        ▼ (실시간 동기화)                                                     │
│   ┌─────────────────────────────────────────────────────────────┐          │
│   │                    Raw Data Layer                           │          │
│   │   • order, review, coupon, users, logistics, artists        │          │
│   │   • 저장: Google Sheets (원본 유지)                          │          │
│   └─────────────────────────────────────────────────────────────┘          │
│        │                                                                    │
│        ▼ (배치: 매일 자정)                                                  │
│   ┌─────────────────────────────────────────────────────────────┐          │
│   │                   1차 가공 Layer                             │          │
│   │   • DailyBasicMetrics (일별 집계)                            │          │
│   │   • DailyReviewMetrics (리뷰 일별 집계)                       │          │
│   │   • DailyCouponMetrics (쿠폰 일별 집계)                       │          │
│   │   • 저장: DB (daily_metrics 테이블)                          │          │
│   └─────────────────────────────────────────────────────────────┘          │
│        │                                                                    │
│        ▼ (배치: 매일 자정 / 온디맨드)                                        │
│   ┌─────────────────────────────────────────────────────────────┐          │
│   │                   2차 가공 Layer                             │          │
│   │   • ProcessedCouponAnalytics                                 │          │
│   │   • ProcessedCustomerAnalytics                               │          │
│   │   • ProcessedOrderPatterns                                   │          │
│   │   • 저장: DB (analytics_* 테이블) + 캐시 (선택적)            │          │
│   └─────────────────────────────────────────────────────────────┘          │
│        │                                                                    │
│        ▼ (API 응답)                                                         │
│   ┌─────────────────────────────────────────────────────────────┐          │
│   │                    Frontend Pages                            │          │
│   │   • 성과 분석 허브: 2차 가공 데이터 직접 사용                  │          │
│   │   • 고객 분석 허브: 2차 가공 데이터 직접 사용                  │          │
│   │   • Business Brain: 2차 가공 데이터 + 추가 분석               │          │
│   └─────────────────────────────────────────────────────────────┘          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 6.3 n차 확장 설계 원칙

#### 3차 이상 분석을 위한 구조적 준비

| 원칙 | 설명 | 구현 방법 |
|------|------|----------|
| **데이터 자산화** | 2차 가공 데이터를 영구 저장하여 ML 학습 데이터로 활용 | 일별 스냅샷 아카이빙 |
| **표준 인터페이스** | 모든 가공 데이터에 동일 메타데이터 구조 적용 | `id`, `createdAt`, `period` 필수 |
| **버전 관리** | 가공 로직 변경 시 이전 데이터와 비교 가능 | `version` 필드 추가 |
| **확장 포인트** | 향후 기능 추가를 위한 여유 필드 | `metadata: JSON` 필드 |

```typescript
// n차 확장을 위한 베이스 인터페이스
interface ProcessedDataBase {
  // 필수 메타데이터
  id: string
  version: string           // 가공 로직 버전
  createdAt: Date
  periodStart: Date
  periodEnd: Date
  
  // 확장 포인트
  metadata: {
    source: string          // 원천 데이터 출처
    processingTime: number  // 처리 소요 시간 (ms)
    recordCount: number     // 원천 레코드 수
    [key: string]: any      // 향후 확장
  }
}

// 3차 확장 예시: 예측 모델 출력
interface PredictedMetrics extends ProcessedDataBase {
  predictions: {
    gmvNextMonth: { value: number; confidence: number }
    churnRiskCustomers: Array<{ userId: string; probability: number }>
  }
  modelVersion: string      // ML 모델 버전
  trainingDataPeriod: { start: Date; end: Date }
}
```

---

## 7. 구현 로드맵

### 7.1 전체 로드맵 개요

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Phase 5 구현 로드맵                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Phase 5-1        Phase 5-2        Phase 5-3        향후                   │
│  (1~2주)          (2~3주)          (3~4주)          (Phase 6+)             │
│  ────────────     ────────────     ────────────     ────────────           │
│  기반 구축        핵심 기능        확장 기능        고도화                  │
│                                                                             │
│  • 데이터 레이어  • 쿠폰 효과 분석  • 고객 360°    • 예측 모델             │
│  • API 기반      • 리뷰 분석      • 주문 패턴     • A/B 테스트             │
│  • IA 재구조화   • 허브 통합      • BB 연동      • 자동화                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 7.2 Phase 5-1: 기반 구축 (1~2주)

#### 개발 범위

| # | 작업 항목 | 산출물 | 데이터 레벨 |
|:-:|----------|--------|:----------:|
| 1 | 1차 가공 데이터 스키마 정의 | DB 스키마 | L1 |
| 2 | 2차 가공 데이터 스키마 정의 | DB 스키마 | L2 |
| 3 | 일별 배치 집계 Job 개발 | `jobs/dailyAggregation.ts` | L1→L2 |
| 4 | 쿠폰 분석 API 개발 | `api/coupon-analytics/*` | L2 |
| 5 | 리뷰 분석 API 개발 | `api/review-analytics/*` | L2 |
| 6 | 메뉴 IA 재구조화 | 사이드바 컴포넌트 수정 | - |

#### 상세 태스크

**Task 1: DB 스키마 생성**

```sql
-- 1차 가공: 일별 기본 지표
CREATE TABLE daily_metrics (
  id SERIAL PRIMARY KEY,
  date DATE NOT NULL UNIQUE,
  order_count INT,
  total_gmv_krw DECIMAL(15,2),
  total_gmv_usd DECIMAL(15,2),
  avg_aov DECIMAL(15,2),
  unique_customers INT,
  new_customers INT,
  returning_customers INT,
  jp_order_count INT,
  jp_gmv DECIMAL(15,2),
  en_order_count INT,
  en_gmv DECIMAL(15,2),
  created_at TIMESTAMP DEFAULT NOW()
);

-- 1차 가공: 리뷰 일별 지표
CREATE TABLE daily_review_metrics (
  id SERIAL PRIMARY KEY,
  date DATE NOT NULL UNIQUE,
  review_count INT,
  avg_rating DECIMAL(3,2),
  rating_1 INT,
  rating_2 INT,
  rating_3 INT,
  rating_4 INT,
  rating_5 INT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 1차 가공: 쿠폰 일별 지표
CREATE TABLE daily_coupon_metrics (
  id SERIAL PRIMARY KEY,
  date DATE NOT NULL UNIQUE,
  issued_count INT,
  used_count INT,
  total_discount DECIMAL(15,2),
  total_gmv_from_coupon DECIMAL(15,2),
  created_at TIMESTAMP DEFAULT NOW()
);

-- 2차 가공: 쿠폰 분석
CREATE TABLE analytics_coupon (
  id SERIAL PRIMARY KEY,
  period_start DATE,
  period_end DATE,
  overall_conversion_rate DECIMAL(5,4),
  overall_roi DECIMAL(10,2),
  rate_conversion DECIMAL(5,4),
  fixed_conversion DECIMAL(5,4),
  jp_conversion DECIMAL(5,4),
  en_conversion DECIMAL(5,4),
  top_performers JSONB,
  under_performers JSONB,
  insights JSONB,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 2차 가공: 고객 분석
CREATE TABLE analytics_customer (
  id SERIAL PRIMARY KEY,
  period_start DATE,
  period_end DATE,
  nps INT,
  promoters_pct DECIMAL(5,2),
  passives_pct DECIMAL(5,2),
  detractors_pct DECIMAL(5,2),
  rfm_summary JSONB,
  cohort_retention JSONB,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 2차 가공: 주문 패턴
CREATE TABLE analytics_order_patterns (
  id SERIAL PRIMARY KEY,
  period_start DATE,
  period_end DATE,
  day_of_week_pattern JSONB,
  quarterly_pattern JSONB,
  gmv_trend_7d JSONB,
  gmv_trend_30d JSONB,
  country_pattern_diff JSONB,
  created_at TIMESTAMP DEFAULT NOW()
);
```

**Task 2: 배치 Job 개발**

```typescript
// backend/src/jobs/dailyAggregation.ts
import cron from 'node-cron'
import { sheetsService } from '../services/sheetsService'
import { db } from '../db'

// 매일 자정 (KST) 실행
cron.schedule('0 0 * * *', async () => {
  console.log('[DailyAggregation] Starting...')
  
  const yesterday = getYesterday()
  
  try {
    // 1차 가공: 기본 지표
    await aggregateDailyMetrics(yesterday)
    
    // 1차 가공: 리뷰 지표
    await aggregateDailyReviewMetrics(yesterday)
    
    // 1차 가공: 쿠폰 지표
    await aggregateDailyCouponMetrics(yesterday)
    
    // 2차 가공: 분석 지표 (최근 30일 기준)
    await generateCouponAnalytics()
    await generateCustomerAnalytics()
    await generateOrderPatterns()
    
    console.log('[DailyAggregation] Completed successfully')
  } catch (error) {
    console.error('[DailyAggregation] Failed:', error)
  }
})

async function aggregateDailyMetrics(date: string) {
  const orders = await sheetsService.getOrdersByDate(date)
  
  const metrics = {
    date,
    order_count: orders.length,
    total_gmv_usd: orders.reduce((sum, o) => sum + o.totalGMV, 0),
    total_gmv_krw: orders.reduce((sum, o) => sum + o.totalGMV, 0) * 1350,
    // ... 기타 집계
  }
  
  await db.query(`
    INSERT INTO daily_metrics (date, order_count, total_gmv_usd, total_gmv_krw, ...)
    VALUES ($1, $2, $3, $4, ...)
    ON CONFLICT (date) DO UPDATE SET ...
  `, [metrics.date, metrics.order_count, ...])
}
```

**Task 3: 메뉴 IA 재구조화**

```typescript
// frontend/config/navigation.ts
export const navigation = [
  {
    name: '홈',
    icon: HomeIcon,
    children: [
      { name: '대시보드', href: '/dashboard' }
    ]
  },
  {
    name: '물류 운영',
    icon: TruckIcon,
    children: [
      { name: '미입고 관리', href: '/unreceived' },
      { name: '분류 추적', href: '/tracking' },
      { name: '물류 관제 센터', href: '/control-tower' }
    ]
  },
  // ... 기존 메뉴 유지
  {
    name: '분석',
    icon: ChartIcon,
    children: [
      {
        name: '성과 분석 허브',      // 🆕 확장
        children: [
          { name: '성과 분석', href: '/analytics' },
          { name: '주문 패턴 분석', href: '/order-patterns', badge: 'NEW' },
          { name: '쿠폰 효과 분석', href: '/coupon-analytics', badge: 'NEW' }
        ]
      },
      {
        name: '고객 분석 허브',      // 🆕 확장
        children: [
          { name: '고객 분석', href: '/customer-analytics' },
          { name: '고객 360° 뷰', href: '/customer-360', badge: 'NEW' },
          { name: '리뷰 분석', href: '/review-analytics', badge: 'NEW' }
        ]
      },
      { name: '작가 분석', href: '/artist-analytics' },
      { name: '비용 & 손익', href: '/cost-analysis' }
    ]
  },
  // ... 나머지 메뉴
]
```

#### 즉시 활용 vs 이후 확장

| 구분 | 항목 | 설명 |
|------|------|------|
| **즉시 활용** | 1차 가공 테이블 | Dashboard, Analytics에서 즉시 조회 가능 |
| **즉시 활용** | 쿠폰/리뷰 분석 API | Phase 5-2 페이지에서 사용 |
| **이후 확장** | 예측 모델 컬럼 | 테이블에 예약 컬럼만 추가, 로직은 향후 |

#### 구조적으로 열어두는 부분

```sql
-- 향후 예측 모델용 컬럼 (Phase 5-1에서는 NULL)
ALTER TABLE analytics_customer ADD COLUMN churn_predictions JSONB;
ALTER TABLE analytics_coupon ADD COLUMN optimal_recommendations JSONB;

-- 향후 A/B 테스트용 테이블 (Phase 5-1에서는 구조만)
CREATE TABLE ab_test_results (
  id SERIAL PRIMARY KEY,
  test_name VARCHAR(100),
  variant VARCHAR(50),
  metrics JSONB,
  created_at TIMESTAMP DEFAULT NOW()
);
```

---

### 7.3 Phase 5-2: 핵심 기능 (2~3주)

#### 개발 범위

| # | 작업 항목 | 산출물 | 데이터 레벨 |
|:-:|----------|--------|:----------:|
| 1 | 쿠폰 효과 분석 페이지 개발 | `/coupon-analytics` | L2 활용 |
| 2 | 리뷰 분석 대시보드 개발 | `/review-analytics` | L2 활용 |
| 3 | 성과/고객 분석 허브 페이지 | 허브 랜딩 페이지 | L2 요약 |
| 4 | Business Brain 쿠폰/리뷰 탭 추가 | BB 페이지 확장 | L2 연동 |

#### 상세 태스크

**Task 1: 쿠폰 효과 분석 페이지**

```typescript
// frontend/app/coupon-analytics/page.tsx
'use client'

import { useQuery } from '@tanstack/react-query'
import { couponAnalyticsApi } from '@/lib/api'
import { KPICard, TrendChart, SegmentTable, InsightCard } from '@/components'

export default function CouponAnalyticsPage() {
  // 2차 가공 데이터 직접 조회
  const { data: analytics } = useQuery({
    queryKey: ['coupon-analytics', dateRange],
    queryFn: () => couponAnalyticsApi.getAnalytics(dateRange)
  })
  
  return (
    <div className="space-y-6">
      {/* KPI 요약 */}
      <div className="grid grid-cols-5 gap-4">
        <KPICard 
          title="전체 전환율" 
          value={formatPercent(analytics?.overallConversionRate)}
          change={analytics?.conversionRateChange}
        />
        <KPICard 
          title="전체 ROI" 
          value={formatNumber(analytics?.overallROI)}
          suffix="x"
          change={analytics?.roiChange}
        />
        {/* ... */}
      </div>
      
      {/* 세그먼트 비교 */}
      <div className="grid grid-cols-2 gap-4">
        <Card title="할인 유형별 성과">
          <SegmentChart 
            data={[
              { name: 'RATE', value: analytics?.rateConversion },
              { name: 'FIXED', value: analytics?.fixedConversion }
            ]}
          />
        </Card>
        <Card title="국가별 성과">
          <SegmentChart 
            data={[
              { name: 'JP', value: analytics?.jpConversion },
              { name: 'EN', value: analytics?.enConversion }
            ]}
          />
        </Card>
      </div>
      
      {/* TOP/하위 쿠폰 */}
      <div className="grid grid-cols-2 gap-4">
        <Card title="🏆 TOP 성과 쿠폰">
          <RankingTable data={analytics?.topPerformers} />
        </Card>
        <Card title="⚠️ 개선 필요 쿠폰">
          <RankingTable data={analytics?.underPerformers} />
        </Card>
      </div>
      
      {/* 자동 생성 인사이트 */}
      <Card title="💡 인사이트">
        {analytics?.insights.map((insight, i) => (
          <InsightCard key={i} {...insight} />
        ))}
      </Card>
    </div>
  )
}
```

**Task 2: Business Brain 연동**

```typescript
// frontend/app/business-brain/page.tsx (기존 확장)

// 새 탭 추가: "쿠폰 인사이트"
const tabs = [
  // ... 기존 탭
  { id: 'coupon', label: '쿠폰 인사이트', icon: TicketIcon },
  { id: 'review', label: '리뷰 인사이트', icon: StarIcon }
]

// 쿠폰 탭 컨텐츠
function CouponInsightsTab() {
  const { data: couponAnalytics } = useQuery({
    queryKey: ['bb-coupon-analytics'],
    queryFn: businessBrainApi.getCouponInsights  // 2차 가공 데이터 활용
  })
  
  return (
    <div className="space-y-4">
      <SummaryCard>
        <h3>쿠폰 전략 요약</h3>
        <p>FIXED 쿠폰이 RATE 대비 전환율 {couponAnalytics?.fixedVsRateRatio}배 높음</p>
        <p>권장: CRM 타겟 FIXED 쿠폰 확대</p>
      </SummaryCard>
      
      <InsightList insights={couponAnalytics?.insights} />
      
      <ActionRecommendations actions={couponAnalytics?.recommendedActions} />
    </div>
  )
}
```

#### 즉시 활용 vs 이후 확장

| 구분 | 항목 | 설명 |
|------|------|------|
| **즉시 활용** | 쿠폰 효과 분석 전체 | 마케팅팀 즉시 활용 가능 |
| **즉시 활용** | 리뷰 분석 기본 기능 | NPS, 트렌드, 작가별 분석 |
| **이후 확장** | 리뷰 키워드 분석 | 일본어 NLP 처리 필요 |
| **이후 확장** | 쿠폰 추천 모델 | ML 모델 개발 필요 |

---

### 7.4 Phase 5-3: 확장 기능 (3~4주)

#### 개발 범위

| # | 작업 항목 | 산출물 | 데이터 레벨 |
|:-:|----------|--------|:----------:|
| 1 | 고객 360° 뷰 개발 | `/customer-360` | L1+L2 통합 |
| 2 | 주문 패턴 분석 개발 | `/order-patterns` | L2 활용 |
| 3 | 허브 간 데이터 연동 | 교차 분석 기능 | L2 공유 |
| 4 | 대시보드 쿠폰/리뷰 KPI 추가 | Dashboard 확장 | L2 활용 |

#### 상세 태스크

**Task 1: 고객 360° 뷰**

```typescript
// frontend/app/customer-360/page.tsx
export default function Customer360Page() {
  const [searchQuery, setSearchQuery] = useState('')
  const [selectedCustomer, setSelectedCustomer] = useState(null)
  
  // 고객 검색 (Raw 데이터 직접 조회)
  const { data: searchResults } = useQuery({
    queryKey: ['customer-search', searchQuery],
    queryFn: () => customerApi.search(searchQuery),
    enabled: searchQuery.length > 2
  })
  
  // 선택된 고객의 통합 정보 (1차 + 2차 가공 데이터 결합)
  const { data: customerDetail } = useQuery({
    queryKey: ['customer-detail', selectedCustomer?.id],
    queryFn: () => customerApi.get360View(selectedCustomer.id),
    enabled: !!selectedCustomer
  })
  
  return (
    <div className="flex gap-6">
      {/* 검색 패널 */}
      <aside className="w-80">
        <SearchInput 
          value={searchQuery} 
          onChange={setSearchQuery} 
          placeholder="고객 ID, 이메일 검색"
        />
        <CustomerList 
          customers={searchResults} 
          onSelect={setSelectedCustomer}
        />
      </aside>
      
      {/* 상세 정보 */}
      <main className="flex-1">
        {customerDetail && (
          <>
            {/* 기본 정보 + RFM 세그먼트 */}
            <CustomerHeader customer={customerDetail.profile} />
            
            {/* 구매 타임라인 */}
            <OrderTimeline orders={customerDetail.orders} />
            
            {/* 쿠폰 사용 이력 */}
            <CouponHistory coupons={customerDetail.couponUsage} />
            
            {/* 리뷰 이력 */}
            <ReviewHistory reviews={customerDetail.reviews} />
            
            {/* 예상 LTV (2차 가공) */}
            <LTVCard ltv={customerDetail.estimatedLTV} />
          </>
        )}
      </main>
    </div>
  )
}
```

#### 구조적으로 열어두는 부분

| 영역 | Phase 5-3에서 준비 | 향후 확장 |
|------|------------------|----------|
| **고객 360°** | 기본 통합 뷰 | 이탈 예측 점수, 추천 상품 |
| **주문 패턴** | 히트맵, 시즈널리티 | 자동 프로모션 트리거 |
| **쿠폰 분석** | 효과 측정 | A/B 테스트 결과 연동 |
| **리뷰 분석** | NPS, 트렌드 | 키워드 추출, 감성 분석 |

---

### 7.5 향후 Phase 6+ 준비 사항

> Phase 5에서는 구현하지 않지만, 구조적으로 열어두는 영역

#### 예측 모델 (Phase 6)

```typescript
// 데이터 구조만 정의, 실제 모델은 향후 개발
interface FuturePredictionFeatures {
  // 고객 이탈 예측
  churnPrediction: {
    probability: number
    riskFactors: string[]
    recommendedAction: string
  }
  
  // GMV 예측
  gmvForecast: {
    nextMonth: number
    confidence: number
  }
  
  // 쿠폰 추천
  couponRecommendation: {
    targetSegment: string
    suggestedType: 'RATE' | 'FIXED'
    expectedROI: number
  }
}
```

#### A/B 테스트 (Phase 6)

```sql
-- 테이블 구조만 생성, 데이터 수집은 향후
CREATE TABLE ab_tests (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100),
  start_date DATE,
  end_date DATE,
  status VARCHAR(20),  -- 'draft', 'running', 'completed'
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE ab_test_variants (
  id SERIAL PRIMARY KEY,
  test_id INT REFERENCES ab_tests(id),
  variant_name VARCHAR(50),
  config JSONB,
  sample_size INT,
  conversion_rate DECIMAL(5,4),
  metrics JSONB
);
```

#### 자동화 트리거 (Phase 6+)

```typescript
// 규칙 기반 자동화 - 인터페이스만 정의
interface AutomationRule {
  id: string
  name: string
  trigger: {
    type: 'threshold' | 'schedule' | 'event'
    condition: string  // e.g., "coupon.conversionRate < 0.01"
  }
  action: {
    type: 'alert' | 'email' | 'slack' | 'coupon_pause'
    config: Record<string, any>
  }
  enabled: boolean
}
```

---

## 8. 부록: 데이터 흐름도

### 8.1 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                              Global Business Hub Data Architecture                       │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                          │
│   ┌─────────────────────────────────────────────────────────────────────────────────┐   │
│   │                           DATA SOURCES (Layer 0: Raw)                            │   │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │   │
│   │  │  order   │ │  review  │ │  coupon  │ │  users   │ │ logistics│ │  artists │ │   │
│   │  │  (102)   │ │   (20)   │ │   (22)   │ │   (10)   │ │   (35)   │ │   (25)   │ │   │
│   │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ │   │
│   └───────┼────────────┼────────────┼────────────┼────────────┼────────────┼────────┘   │
│           │            │            │            │            │            │            │
│           └────────────┴────────────┴─────┬──────┴────────────┴────────────┘            │
│                                           │                                              │
│                                           ▼                                              │
│   ┌─────────────────────────────────────────────────────────────────────────────────┐   │
│   │                         PROCESSING LAYER (Layer 1: 1차 가공)                     │   │
│   │                                                                                  │   │
│   │   [Daily Batch Job] ──────────────────────────────────────────────────────────  │   │
│   │         │                                                                        │   │
│   │         ├─► daily_metrics (일별 기본 집계)                                       │   │
│   │         ├─► daily_review_metrics (리뷰 일별 집계)                                │   │
│   │         └─► daily_coupon_metrics (쿠폰 일별 집계)                                │   │
│   │                                                                                  │   │
│   └─────────────────────────────────────────┬───────────────────────────────────────┘   │
│                                             │                                            │
│                                             ▼                                            │
│   ┌─────────────────────────────────────────────────────────────────────────────────┐   │
│   │                        ANALYTICS LAYER (Layer 2: 2차 가공)                       │   │
│   │                                                                                  │   │
│   │   ┌───────────────────┐  ┌───────────────────┐  ┌───────────────────┐          │   │
│   │   │ ProcessedCoupon   │  │ ProcessedCustomer │  │ ProcessedOrder    │          │   │
│   │   │ Analytics         │  │ Analytics         │  │ Patterns          │          │   │
│   │   │ ───────────────── │  │ ───────────────── │  │ ───────────────── │          │   │
│   │   │ • 전환율          │  │ • NPS             │  │ • 요일 패턴       │          │   │
│   │   │ • ROI             │  │ • RFM 요약        │  │ • 분기 시즈널리티 │          │   │
│   │   │ • 세그먼트별 성과 │  │ • 코호트 리텐션   │  │ • 국가별 차이     │          │   │
│   │   │ • 자동 인사이트   │  │ • 세그먼트 이동   │  │ • 트렌드          │          │   │
│   │   └─────────┬─────────┘  └─────────┬─────────┘  └─────────┬─────────┘          │   │
│   │             │                      │                      │                     │   │
│   └─────────────┼──────────────────────┼──────────────────────┼─────────────────────┘   │
│                 │                      │                      │                         │
│                 └──────────────────────┼──────────────────────┘                         │
│                                        │                                                 │
│                                        ▼                                                 │
│   ┌─────────────────────────────────────────────────────────────────────────────────┐   │
│   │                            APPLICATION LAYER                                     │   │
│   │                                                                                  │   │
│   │   ┌─────────────────────────────────────────────────────────────────────────┐   │   │
│   │   │                         성과 분석 허브                                   │   │   │
│   │   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                     │   │   │
│   │   │   │ 성과 분석   │  │ 주문 패턴   │  │ 쿠폰 효과   │                     │   │   │
│   │   │   │ (기존)      │  │ (신규)      │  │ (신규)      │                     │   │   │
│   │   │   └─────────────┘  └─────────────┘  └─────────────┘                     │   │   │
│   │   └─────────────────────────────────────────────────────────────────────────┘   │   │
│   │                                                                                  │   │
│   │   ┌─────────────────────────────────────────────────────────────────────────┐   │   │
│   │   │                         고객 분석 허브                                   │   │   │
│   │   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                     │   │   │
│   │   │   │ 고객 분석   │  │ 고객 360°   │  │ 리뷰 분석   │                     │   │   │
│   │   │   │ (기존)      │  │ (신규)      │  │ (신규)      │                     │   │   │
│   │   │   └─────────────┘  └─────────────┘  └─────────────┘                     │   │   │
│   │   └─────────────────────────────────────────────────────────────────────────┘   │   │
│   │                                                                                  │   │
│   │   ┌──────────────────┐                                                          │   │
│   │   │  Business Brain  │ ← 모든 2차 가공 데이터 통합 활용                          │   │
│   │   └──────────────────┘                                                          │   │
│   │                                                                                  │   │
│   └──────────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                          │
│   ┌─────────────────────────────────────────────────────────────────────────────────┐   │
│   │                       FUTURE LAYER (Layer 3+: n차 가공) [Phase 6+]               │   │
│   │                                                                                  │   │
│   │   ┌───────────────┐  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐   │   │
│   │   │ 예측 모델     │  │ A/B 테스트    │  │ 자동화 규칙   │  │ ML 파이프라인 │   │   │
│   │   │ (구조만 준비) │  │ (구조만 준비) │  │ (구조만 준비) │  │ (구조만 준비) │   │   │
│   │   └───────────────┘  └───────────────┘  └───────────────┘  └───────────────┘   │   │
│   │                                                                                  │   │
│   └──────────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                          │
└──────────────────────────────────────────────────────────────────────────────────────────┘
```

### 8.2 데이터 재사용 매트릭스

| 2차 가공 데이터 | Dashboard | 성과 허브 | 고객 허브 | Business Brain |
|----------------|:---------:|:--------:|:--------:|:--------------:|
| ProcessedCouponAnalytics | ○ (요약) | ● (전체) | - | ● (인사이트) |
| ProcessedCustomerAnalytics | ○ (NPS) | - | ● (전체) | ● (인사이트) |
| ProcessedOrderPatterns | ○ (트렌드) | ● (전체) | - | ● (인사이트) |

- ●: 전체 데이터 활용
- ○: 요약/일부 데이터 활용
- -: 미사용

---

> **문서 관리자**: AI 자동화팀  
> **최종 업데이트**: 2024-12-17  
> **다음 리뷰 예정일**: Phase 5-1 완료 후  
> **관련 문서**: `데이터_규칙_가이드.md`, `허브_개선_제안서.md`

