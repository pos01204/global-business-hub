# 📖 Global Business Hub 데이터 규칙 가이드

> **문서 버전**: 1.0  
> **작성일**: 2024-12-17  
> **목적**: 모든 작업 전 **필수 숙지** 사항 - 공통 규칙, 데이터 정의, 상수 값  
> **⚠️ 중요**: 이 문서의 규칙은 모든 페이지와 컴포넌트에서 **반드시 준수**해야 합니다

---

## 📋 Quick Reference (빠른 참조)

### 🔑 핵심 규칙 체크리스트

| 규칙 | 상세 | 필수 여부 |
|------|------|:--------:|
| **환율** | 1 USD = 1,350 KRW (고정) | ✅ 필수 |
| **원화 포맷** | `₩1,234,567` (정수, 천단위 쉼표) | ✅ 필수 |
| **변화율** | `+15.0%` / `-3.2%` (소수점 1자리) | ✅ 필수 |
| **Null 처리** | `safeNumber()`, `safeString()` 사용 | ✅ 필수 |
| **날짜 포맷 (API)** | `YYYY-MM-DD` | ✅ 필수 |
| **타임존** | Asia/Seoul (KST, UTC+9) | ✅ 필수 |
| **고객 활동 기준** | Active ≤90일, Inactive 91~180일, Churn 181일+ | ✅ 필수 |
| **캐싱 전략** | REALTIME(1분), ANALYTICS(5분), REFERENCE(1시간) | ✅ 필수 |

### 📁 필수 파일 Import 경로

```typescript
// 모든 페이지에서 아래 모듈을 import하여 사용
import { formatCurrency, formatChange, toKRW } from '@/lib/formatters'
import { safeNumber, safeString, safeDivide } from '@/lib/safeUtils'
import { CURRENCY, TIMEZONE } from '@/config/constants'
import { CUSTOMER_ACTIVITY_THRESHOLDS, LOGISTICS_CRITICAL_DAYS } from '@/config/businessRules'
```

---

## 📋 목차

1. [환율 변환 규칙](#1-환율-변환-규칙)
2. [통화 포맷팅 규칙](#2-통화-포맷팅-규칙)
3. [변화율 계산 규칙](#3-변화율-계산-규칙)
4. [Null/Undefined 처리 규칙](#4-nullundefined-처리-규칙)
5. [날짜/시간 처리 규칙](#5-날짜시간-처리-규칙)
6. [비즈니스 규칙 상수](#6-비즈니스-규칙-상수)
7. [데이터 캐싱 규칙](#7-데이터-캐싱-규칙)
8. [페이지네이션 규칙](#8-페이지네이션-규칙)
9. [데이터 엔티티 정의](#9-데이터-엔티티-정의)
10. [부록: 데이터 시트 상세 정의](#10-부록-데이터-시트-상세-정의)

---

## 1. 환율 변환 규칙

### 📌 기본 환율 설정

| 항목 | 값 | 비고 |
|------|-----|------|
| **기준 환율** | **1 USD = 1,350 KRW** | 2024년 12월 기준 |
| **적용 시점** | 데이터 표시 시점 | 실시간 환율 X |
| **반올림 규칙** | 원화 기준 정수 반올림 | `Math.round()` |

### 📌 구현 코드

```typescript
// config/constants.ts
export const CURRENCY = {
  USD_TO_KRW: 1350,              // 고정 환율
  CURRENCY_SYMBOL_KRW: '₩',
  CURRENCY_SYMBOL_USD: '$',
  DECIMAL_PLACES_KRW: 0,         // 원화는 소수점 없음
  DECIMAL_PLACES_USD: 2,         // 달러는 소수점 2자리
} as const

// lib/formatters.ts
export const toKRW = (usdAmount: number | null | undefined): number => {
  if (usdAmount === null || usdAmount === undefined || isNaN(usdAmount)) return 0
  return Math.round(usdAmount * CURRENCY.USD_TO_KRW)
}

export const toUSD = (krwAmount: number | null | undefined): number => {
  if (krwAmount === null || krwAmount === undefined || isNaN(krwAmount)) return 0
  return Math.round((krwAmount / CURRENCY.USD_TO_KRW) * 100) / 100
}
```

### 📌 사용 예시

```typescript
// ✅ 올바른 사용
import { toKRW, CURRENCY } from '@/lib/formatters'
const krwValue = toKRW(97.29)  // → 131,341

// ❌ 잘못된 사용
const USD_TO_KRW = 1350  // 페이지에 하드코딩 금지!
```

---

## 2. 통화 포맷팅 규칙

### 📌 필수 포맷팅 함수

```typescript
// lib/formatters.ts

/**
 * 원화 포맷팅 (KRW)
 * @param value - 금액 (number | string | null | undefined)
 * @param defaultValue - 기본값 (default: '₩0')
 * @returns 포맷팅된 문자열 (예: '₩1,234,567')
 */
export const formatCurrency = (
  value: number | string | null | undefined,
  defaultValue: string = '₩0'
): string => {
  // null, undefined 체크
  if (value === null || value === undefined) return defaultValue
  
  // 문자열 → 숫자 변환 (쉼표, 공백 제거)
  const numValue = typeof value === 'string' 
    ? parseFloat(value.replace(/[,\s₩]/g, '')) 
    : value
  
  // NaN, Infinity 체크
  if (isNaN(numValue) || !isFinite(numValue)) return defaultValue
  
  return `₩${Math.round(numValue).toLocaleString('ko-KR')}`
}

/**
 * 달러 포맷팅 (USD)
 */
export const formatUSD = (value: number | null | undefined): string => {
  if (value === null || value === undefined || isNaN(value)) return '$0.00'
  return `$${value.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`
}

/**
 * USD → KRW 변환 후 포맷팅
 */
export const formatKRW = (usdValue: number | null | undefined): string => {
  return formatCurrency(toKRW(usdValue))
}
```

### 📌 포맷 예시

| 입력 | 출력 | 설명 |
|------|------|------|
| `1234567` | `₩1,234,567` | 기본 포맷 |
| `null` | `₩0` | null 처리 |
| `undefined` | `₩0` | undefined 처리 |
| `NaN` | `₩0` | NaN 처리 |
| `"1,234"` | `₩1,234` | 문자열 처리 |
| `97.29` (USD) | `₩131,341` | USD → KRW 변환 |

---

## 3. 변화율 계산 규칙

### 📌 계산 공식

```typescript
// lib/formatters.ts

/**
 * 변화율 계산
 * @returns 변화율 (소수점 비율, 예: 0.15 = 15%)
 */
export const calculateChangeRate = (
  current: number | null | undefined,
  previous: number | null | undefined
): number => {
  const curr = safeNumber(current)
  const prev = safeNumber(previous)
  
  if (prev === 0) return curr > 0 ? Infinity : 0
  return (curr - prev) / prev
}

/**
 * 변화율 포맷팅
 * @returns 포맷팅된 문자열 (예: '+15.0%', '-3.2%', 'New')
 */
export const formatChange = (change: number): string => {
  if (change === Infinity) return 'New'
  if (isNaN(change) || !isFinite(change)) return '-'
  const sign = change >= 0 ? '+' : ''
  return `${sign}${(change * 100).toFixed(1)}%`
}
```

### 📌 색상 규칙

| 변화율 | 색상 | CSS 클래스 (Light/Dark) |
|--------|------|------------------------|
| 양수 (+) | 🟢 초록 | `text-green-600` / `text-green-400` |
| 음수 (-) | 🔴 빨강 | `text-red-600` / `text-red-400` |
| 0 또는 N/A | ⚫ 회색 | `text-gray-500` / `text-gray-400` |
| New | 🔵 파랑 | `text-blue-600` / `text-blue-400` |

---

## 4. Null/Undefined 처리 규칙

### 📌 안전 유틸리티 함수

```typescript
// lib/safeUtils.ts

/** 안전한 숫자 변환 */
export const safeNumber = (value: unknown, defaultValue: number = 0): number => {
  if (value === null || value === undefined) return defaultValue
  const num = typeof value === 'string' ? parseFloat(value) : Number(value)
  return isNaN(num) || !isFinite(num) ? defaultValue : num
}

/** 안전한 문자열 변환 */
export const safeString = (value: unknown, defaultValue: string = ''): string => {
  if (value === null || value === undefined) return defaultValue
  return String(value)
}

/** 안전한 배열 합계 */
export const safeSum = (arr: (number | null | undefined)[]): number => 
  arr.reduce((acc, val) => acc + safeNumber(val), 0)

/** 안전한 평균 계산 */
export const safeAverage = (arr: (number | null | undefined)[]): number => {
  const validValues = arr.filter(v => v !== null && v !== undefined && !isNaN(Number(v)))
  if (validValues.length === 0) return 0
  return safeSum(validValues) / validValues.length
}

/** 0 나누기 방지 함수 */
export const safeDivide = (
  numerator: number | null | undefined,
  denominator: number | null | undefined,
  defaultValue: number = 0
): number => {
  const num = safeNumber(numerator)
  const den = safeNumber(denominator)
  if (den === 0) return defaultValue
  return num / den
}
```

### 📌 사용 예시

```typescript
// ✅ 올바른 사용
import { safeNumber, safeDivide } from '@/lib/safeUtils'

const value = safeNumber(data?.total)  // null이면 0
const rate = safeDivide(sales, orders)  // 0으로 나누기 방지

// ❌ 잘못된 사용
const value = data.total  // null 체크 없음
const rate = sales / orders  // 0 나누기 위험
```

---

## 5. 날짜/시간 처리 규칙

### 📌 날짜 포맷 표준

| 용도 | 포맷 | 예시 |
|------|------|------|
| **API 통신** | `YYYY-MM-DD` | `2024-12-17` |
| **시간 포함** | `YYYY-MM-DD HH:mm:ss` | `2024-12-17 14:30:00` |
| **UI 표시 (한국어)** | `YYYY년 MM월 DD일` | `2024년 12월 17일` |
| **UI 표시 (간략)** | `MM/DD` | `12/17` |
| **기간 표시** | `YYYY-MM` | `2024-12` |
| **ISO 8601** | `YYYY-MM-DDTHH:mm:ss.sssZ` | `2024-12-17T05:30:00.000Z` |

### 📌 타임존 규칙

```typescript
// config/constants.ts
export const TIMEZONE = {
  DEFAULT: 'Asia/Seoul',  // 기본 타임존 (KST)
  UTC_OFFSET: 9,          // UTC+9
} as const

// 모든 날짜 비교는 KST 기준으로 수행
```

---

## 6. 비즈니스 규칙 상수

### 📌 고객 활동 상태 기준

```typescript
// config/businessRules.ts
export const CUSTOMER_ACTIVITY_THRESHOLDS = {
  ACTIVE_DAYS: 90,        // 활성: 최근 90일 내 구매
  INACTIVE_DAYS: 180,     // 비활성: 91~180일 내 구매
  CHURN_RISK_DAYS: 181,   // 이탈 위험: 181일+ 미구매
} as const

export type CustomerActivityStatus = 'Active' | 'Inactive' | 'Churn Risk'
```

### 📌 물류 위험 기준일

```typescript
// config/businessRules.ts
export const LOGISTICS_CRITICAL_DAYS = {
  unreceived: 7,            // 미입고: 결제 후 7일
  artistShipping: 5,        // 국내배송: 결제 후 5일
  awaitingInspection: 2,    // 검수대기: 결제 후 2일
  inspectionComplete: 3,    // 포장대기: 결제 후 3일
  internationalShipping: 14, // 국제배송: 결제 후 14일
} as const
```

### 📌 물류 상태값 매핑

> 상세 매핑은 [10.1 Order-Shipment Status 규칙](#101-order-shipment-status-규칙) 참조

| 도메인 | 주요 상태값 | UI 표시 (한글) | 색상 |
|--------|------------|---------------|------|
| **Order** | `COMPLETED` | 주문 완료 | 🟢 Green |
| **Order** | `CANCELLED` | 취소 | 🔴 Red |
| **Shipment Item** | `DELIVERED` | 배송완료 | 🟢 Green |
| **Shipment Item** | `INSPECTION_COMPLETE` | 검수완료 | 🔵 Blue |
| **Desire** | `INSPECTION_FAIL` | 작품 반송 | 🔴 Red |
| **Global** | `DELIVERY_COMPLETE` | 배송완료 | 🟢 Green |

---

## 7. 데이터 캐싱 규칙

### 📌 React Query `staleTime` 표준

| 데이터 유형 | staleTime | cacheTime | 적용 API |
|------------|-----------|-----------|----------|
| **실시간** | 1분 | 5분 | control-tower, dashboard-tasks |
| **분석** | 5분 | 30분 | analytics, artist-analytics, business-brain |
| **참조** | 1시간 | 24시간 | rates, countries, carriers |
| **정적** | 24시간 | 7일 | schema, constants |

### 📌 구현 코드

```typescript
// config/cacheStrategy.ts
export const CACHE_STRATEGY = {
  REALTIME: { staleTime: 1 * 60 * 1000, cacheTime: 5 * 60 * 1000 },
  ANALYTICS: { staleTime: 5 * 60 * 1000, cacheTime: 30 * 60 * 1000 },
  REFERENCE: { staleTime: 60 * 60 * 1000, cacheTime: 24 * 60 * 60 * 1000 },
  STATIC: { staleTime: 24 * 60 * 60 * 1000, cacheTime: 7 * 24 * 60 * 60 * 1000 },
} as const
```

---

## 8. 페이지네이션 규칙

### 📌 표준 설정

```typescript
// config/pagination.ts
export const PAGINATION = {
  DEFAULT_PAGE_SIZE: 50,
  MAX_PAGE_SIZE: 100,
  INITIAL_LOAD_SIZE: 20,
  ALLOWED_PAGE_SIZES: [10, 20, 50, 100],
} as const
```

### 📌 API 응답 형식

```typescript
interface PaginatedResponse<T> {
  data: T[]
  pagination: {
    page: number
    pageSize: number
    totalCount: number
    totalPages: number
    hasNext: boolean
    hasPrev: boolean
  }
}
```

---

## 9. 데이터 엔티티 정의

### 📐 엔티티 관계 다이어그램 (ERD)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           GLOBAL BUSINESS HUB ERD                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐        ┌─────────────┐        ┌─────────────┐             │
│  │   USERS     │ 1    N │   ORDERS    │ 1    N │  LOGISTICS  │             │
│  │─────────────│───────>│─────────────│───────>│─────────────│             │
│  │ ID (PK)     │        │ order_code  │        │ shipment_   │             │
│  │ NAME        │        │ (PK)        │        │ item_id(PK) │             │
│  │ EMAIL       │        │ user_id(FK) │        │ order_code  │             │
│  │ COUNTRY     │        │ Total GMV   │        │ (FK)        │             │
│  │ CREATED_AT  │        │ shipment_id │        │ country     │             │
│  └─────────────┘        └─────────────┘        │ logistics   │             │
│        │                       │               │ artist_name │             │
│        │                       │               │ Total GMV   │             │
│        │                       │               └─────────────┘             │
│        │                       │                      │                    │
│        │                       │                      │ N                  │
│        │                       │                      ▼                    │
│        │                       │               ┌─────────────┐             │
│        │                       │           N   │   ARTISTS   │             │
│        │                       └──────────────>│─────────────│             │
│        │                                       │ 작가 ID(PK) │             │
│        │                                       │ 작가명(KR)  │             │
│        │                                       │ 작가명(GL)  │             │
│        │                                       │ mail        │             │
│        ▼                                       │ Live 작품수 │             │
│  ┌─────────────┐                               └─────────────┘             │
│  │ USER_LOCALE │                                      │                    │
│  │─────────────│                                      │                    │
│  │ user_id(FK) │                                      │                    │
│  │ timezone    │        ┌─────────────┐               │                    │
│  │ country_code│        │   REVIEW    │               │                    │
│  │ region      │        │─────────────│<──────────────┘                    │
│  └─────────────┘        │ review_id   │                                    │
│                         │ (PK)        │                                    │
│                         │ rating      │                                    │
│                         │ contents    │                                    │
│                         │ artist_id   │                                    │
│                         │ (FK)        │                                    │
│                         │ product_id  │                                    │
│                         │ user_id(FK) │                                    │
│                         └─────────────┘                                    │
│                                                                             │
│  ┌─────────────┐        ┌─────────────┐        ┌─────────────┐             │
│  │ SETTLEMENT  │        │  RATE_*     │        │  QC 시트들  │             │
│  │ _RECORDS    │ 참조   │─────────────│        │─────────────│             │
│  │─────────────│───────>│ country     │        │ 한글_raw    │             │
│  │ id (PK)     │        │ weight_from │        │ OCR_결과    │             │
│  │ shipment_id │        │ weight_to   │        │ archiving   │             │
│  │ (FK)        │        │ rate        │        └─────────────┘             │
│  │ carrier     │        └─────────────┘                                    │
│  │ total_cost  │                                                           │
│  └─────────────┘                                                           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 📊 핵심 엔티티 요약

| 엔티티 | 시트명 | PK | 주요 FK | 데이터 건수 |
|--------|--------|-----|---------|:-----------:|
| **주문** | order | order_code | user_id → users.ID | ~1,780 |
| **물류** | logistics | shipment_item_id | order_code → order | ~4,314 |
| **사용자** | users | ID | - | ~73,837 |
| **작가** | artists | 작가 ID (Global) | - | ~3,852 |
| **리뷰** | review | review_id | artist_id, user_id | ~657 |
| **정산** | Settlement_records | id | shipment_id → logistics | ~123 |
| **🆕 쿠폰** | coupon | coupon_id | - | ~673 |

---

## 10. 부록: 데이터 시트 상세 정의

### 10.1 Order-Shipment Status 규칙

> Order-shipment status 규칙 문서에 따른 도메인별 상태 매핑

| 작가 상태 | 구매자 상태 | Order Status | Shipment Item Status | Global Status | Trigger |
|----------|-----------|--------------|---------------------|---------------|---------|
| - | - | CREATED | - | - | 주문 생성 |
| 취소 | Cancelled | CANCELLED | - | - | 주문 취소 |
| 결제완료 | Payment complete | ORDER_COMPLETED | CREATED | - | 주문 완료 |
| 입고 발송 | Item sent | - | ARTIST_SENT | IN_DELIVERY | 작가 송장 입력 |
| 작품 반송 | - | - | INSPECTION_FAIL | - | 검수 실패 |
| 검수완료 | Item sent | - | INSPECTION_COMPLETE | INSPECTION_COMPLETE | 검수완료 |
| 배송중 | In Transit | - | DELIVERING | SHIPPED | 해외운송장 발급 |
| 배송완료 | Delivered | COMPLETED | DELIVERED | DELIVERY_COMPLETE | 배송완료 |
| ? | Refunded | REFUNDED | - | - | 환불 처리 |

---

### 10.2 Google Sheets 시트 매핑

| 시트명 | 설명 | 주요 소비 페이지 | 데이터 건수 |
|--------|------|-----------------|:-----------:|
| order | 주문 정보 | Dashboard, Analytics, Business Brain | ~1,780 |
| logistics | 물류 추적 (아이템 단위) | Control Tower, Settlement, Cost Analysis | ~4,314 |
| users | 사용자 정보 | Analytics, Business Brain | ~73,837 |
| artists | 작가 정보 | Artist Analytics, Business Brain | ~3,852 |
| artists_mail | 작가 이메일 | QC, Sopo Receipt | ~4,530 |
| review | 리뷰 정보 | Reviews | ~657 |
| Settlement_records | 정산 기록 | Settlement, Cost Analysis | ~123 |
| **🆕 coupon** | **쿠폰 정보** | **Analytics, Business Brain, 쿠폰 분석** | **~673** |
| Sopo_tracking | 소포수령증 트래킹 | Sopo Receipt | ~86 |
| Sopo_jotform | JotForm 제출 | Sopo Receipt | ~462 |
| [QC] 한글_raw | 텍스트 QC | QC | ~7,004 |
| [QC] OCR_결과_raw | 이미지 OCR | QC | ~4,858 |
| [QC] archiving | QC 아카이브 | QC | ~1,375 |
| user_locale | 사용자 지역 정보 | Analytics | ~79,163 |
| Rate_* | 요금표 | Cost Analysis, Settlement | 참조용 |

---

### 10.3 API 엔드포인트 목록

| 카테고리 | 엔드포인트 | 설명 | 캐시 전략 |
|---------|-----------|------|----------|
| Dashboard | `/api/dashboard/main` | 메인 대시보드 KPI | ANALYTICS |
| Dashboard | `/api/dashboard/tasks` | 오늘 할 일 | REALTIME |
| Analytics | `/api/analytics` | 성과 분석 데이터 | ANALYTICS |
| Control Tower | `/api/control-tower` | 물류 파이프라인 | REALTIME |
| Artist | `/api/artist-analytics/*` | 작가 분석 | ANALYTICS |
| Business Brain | `/api/business-brain/*` | AI 인사이트 | ANALYTICS |
| Cost | `/api/cost-analysis/*` | 비용 분석 | ANALYTICS |
| Settlement | `/api/settlement/*` | 물류비 정산 | ANALYTICS |
| Reviews | `/api/reviews/*` | 리뷰 갤러리 | ANALYTICS |
| QC | `/api/qc/*` | QC 관리 | ANALYTICS |
| Sopo | `/api/sopo-receipt/*` | 소포수령증 | ANALYTICS |

---

### 10.4 Order 시트 컬럼 정의 (27개)

> **Raw Data 출처**: `[GB] 대시보드 제작 - order.csv`  
> **Primary Key**: `order_code`

#### 기본 식별 필드 (A~H)

| 컬럼 | 컬럼명 | 타입 | 활용 여부 | 설명 |
|:----:|--------|------|:--------:|------|
| A | `order_created` | date | ✅ YES | 주문 생성 일시 |
| B | `order_created의 분기` | string | ❌ NO | 파생: 주문의 분기 |
| C | `order_created의 월` | string | ❌ NO | 파생: 주문의 월 |
| D | `주: order_created` | string | ❌ NO | 파생: 주문의 주차 |
| E | `order_created의 요일` | string | ❌ NO | 파생: 주문의 요일 |
| F | `order_id` | number | ✅ YES | 주문 ID |
| G | `user_id` | number | ✅ YES | 사용자 ID (FK) |
| H | `order_code` | string | ✅ YES | 주문 고유 식별자 (PK) |

#### 결제 정보 필드 (I~P)

| 컬럼 | 컬럼명 | 타입 | 활용 여부 | 설명 |
|:----:|--------|------|:--------:|------|
| I | `PG사` | string | ✅ YES | 결제 대행사 (PAYMENTWALL, PAYPAL) |
| J | `method` | string | ✅ YES | 결제 방법 (CARD) |
| K | `order_status` | string | ✅ YES | 주문 상태 (COMPLETED 등) |
| L | `shipment_id` | number | ✅ YES | 배송 ID (FK) |
| M | `shipment status` | string | ✅ YES | 배송 상태 |
| N | `country` | string | ✅ YES | 배송 국가 코드 |
| O | `platform` | string | ✅ YES | 주문 플랫폼 (iOS, Android) |
| P | `결제 통화` | string | ❌ NO | 결제 통화 (USD 고정) |

#### 금액 필드 (Q~AA)

| 컬럼 | 컬럼명 | 타입 | 활용 여부 | 설명 |
|:----:|--------|------|:--------:|------|
| Q | `작품 가격` | number | ❌ NO | 작품 단가 (USD) |
| R | `옵션 가격` | number | ❌ NO | 옵션 추가 금액 (USD) |
| S | `Hidden Fee` | number | ❌ NO | 숨김 수수료 (USD) |
| T | `Product GMV` | number | ✅ YES | 상품 GMV (USD) |
| U | `고객 부담 배송비 (Item)` | number | ✅ YES | 고객 부담 배송비 (USD) |
| **V** | **`Total GMV`** | number | ✅ **YES** | **총 거래액 (USD) - 핵심 KPI** |
| W | `아이디어스 쿠폰비 (Item)` | number | ❌ NO | 쿠폰 할인액 (USD) |
| X | `Pay GMV` | number | ✅ YES | 실 결제 금액 (USD) |
| Y | `구매수량` | number | ✅ YES | 구매 수량 |
| Z | `작품 판매 금액(KRW)` | number | ✅ YES | 작품 판매 금액 (KRW) |
| AA | `PG 수수료` | number | ✅ YES | PG 수수료 (USD) |

---

### 10.5 Logistics 시트 컬럼 정의 (43개) ⭐

> **Raw Data 출처**: `[GB] 대시보드 제작 - logistics.csv`  
> **Primary Key**: `shipment_item_id`  
> **⭐ 핵심 시트**: 물류 추적의 중심 데이터

#### 기본 식별 필드 (A~H)

| 컬럼 | 컬럼명 | 타입 | 활용 여부 | 설명 |
|:----:|--------|------|:--------:|------|
| A | `order_created` | date | ✅ YES | 주문 생성 일시 |
| B | `order_id` | number | ✅ YES | 주문 ID |
| C | `user_id` | number | ✅ YES | 사용자 ID (FK) |
| D | `order_code` | string | ✅ YES | 주문 코드 (FK) |
| E | `order_item_id` | number | ✅ YES | 주문 아이템 ID |
| F | `shipment_id` | number | ✅ YES | 배송 ID |
| G | `shipment_item_id` | number | ✅ YES | 배송 아이템 ID (PK) |
| H | `country` | string | ✅ YES | 배송 국가 |

#### 상태 필드 (I~U) - Order-shipment status 규칙 참조

| 컬럼 | 컬럼명 | 타입 | 활용 여부 | 설명 |
|:----:|--------|------|:--------:|------|
| I | `logistics` | string | ✅ YES | 물류 상태 (배송완료 등) |
| J | `order item status` | string | ✅ YES | Order-item 상태 |
| K | `global bundle status` | string | ✅ YES | Global 상태 |
| L | `artist_name` | string | ✅ YES | 작가명 (영문) |
| **M** | **`artist_name (kr)`** | string | ✅ **YES** | **작가명 (한글) - 핵심 필드** |
| N | `작가 발송 상태` | string | ✅ YES | Desire 상태 |
| O | `작가 발송 택배사` | string | ✅ YES | 국내 택배사 |
| P | `작가 발송 송장번호` | string | ✅ YES | 국내 송장번호 |
| Q~T | 날짜 필드들 | date | ✅ YES | 각종 날짜 |
| U | `shipment item status` | string | ✅ YES | Shipment Item 상태 |

#### 금액 필드 (AM~AQ)

| 컬럼 | 컬럼명 | 타입 | 활용 여부 | 설명 |
|:----:|--------|------|:--------:|------|
| **AM** | **`Total GMV`** | number | ✅ **YES** | **총 거래액 (USD) - 핵심 KPI** |
| AN | `아이디어스 쿠폰비 (Item)` | number | ❌ NO | 쿠폰 할인액 |
| AO | `Pay GMV` | number | ✅ YES | 실 결제 금액 |
| AP | `구매수량` | number | ✅ YES | 구매 수량 |
| AQ | `작품 판매 금액(KRW)` | number | ✅ YES | 판매 금액 (KRW) |

---

### 10.6 Users 시트 컬럼 정의 (7개)

> **Raw Data 출처**: `[GB] 대시보드 제작 - users.csv`  
> **Primary Key**: `ID`

| 컬럼 | 컬럼명 | 타입 | 활용 여부 | 설명 |
|:----:|--------|------|:--------:|------|
| A | `ID` | number | ✅ YES | 사용자 고유 식별자 (PK) |
| B | `NAME` | string | ❌ NO | 사용자 이름 (개인정보) |
| C | `EMAIL` | string | ❌ NO | 이메일 주소 (개인정보) |
| D | `PHONE_NUMBER` | string | ❌ NO | 전화번호 (대부분 빈값) |
| E | `COUNTRY` | string | ✅ YES | 국가 코드 |
| F | `CREATED_AT` | datetime | ✅ YES | 가입 일시 (코호트 분석) |
| G | `UPDATED_AT` | datetime | ❌ NO | 정보 수정 일시 |

---

### 10.7 Artists 시트 컬럼 정의 (20개)

> **Raw Data 출처**: `[GB] 대시보드 제작 - artists.csv`  
> **Primary Key**: `작가 ID (Global)`

| 컬럼 | 컬럼명 | 타입 | 활용 여부 | 설명 |
|:----:|--------|------|:--------:|------|
| A | `작가 ID (Global)` | number | ✅ YES | 글로벌 작가 ID (PK) |
| B | `작가명 (Global)` | string | ✅ YES | 작가명 (영문) |
| C | `사업자 이름 (Global)` | string | ❌ NO | 사업자 등록명 |
| D | `작가 ID (KR)` | number | ✅ YES | KR 작가 ID |
| E | `작가 UUID (KR)` | string | ✅ YES | KR 작가 UUID |
| **F** | **`작가명 (KR)`** | string | ✅ **YES** | **작가명 (한글) - 핵심 필드** |
| G | `작가 상태 (Global)` | string | ✅ YES | 상태 (ACTIVE, VACATION) |
| H~M | KR 관련 필드 | - | ❌ NO | 대부분 미활용 |
| N~S | 작품 수 필드 | number | 일부 YES | Live 작품수 활용 |
| T | `mail` | string | ✅ YES | 작가 이메일 |

---

### 10.8 Review 시트 컬럼 정의 (15개) ⚠️

> **Raw Data 출처**: `[GB] 대시보드 제작 - review.csv`  
> **Primary Key**: `review_id`  
> **⚠️ 현재 미활용**: 고활용 잠재력 보유

| 컬럼 | 컬럼명 | 타입 | 활용 여부 | 설명 |
|:----:|--------|------|:--------:|------|
| A | `dt` | date | ❌ NO → **활용 권장** | 리뷰 작성일 |
| B | `review_id` | number | ❌ NO | 리뷰 ID (PK) |
| **C** | **`rating`** | number | ❌ NO → **활용 권장** | **평점 (10점 만점) - NPS 산출** |
| **D** | **`contents`** | string | ❌ NO → **활용 권장** | **리뷰 내용 - 키워드 분석** |
| E | `contents_len` | number | ❌ NO | 리뷰 길이 |
| F | `image_url` | string | ❌ NO | 이미지 URL |
| G | `image_cnt` | number | ❌ NO | 이미지 개수 |
| H | `product_id` | number | ❌ NO → 활용 권장 | 상품 ID |
| I | `product_name` | string | ❌ NO | 상품명 |
| **J** | **`artist_id`** | number | ❌ NO → **활용 권장** | **작가 ID - 작가별 분석** |
| K | `artist_name` | string | ❌ NO | 작가명 |
| L | `user_id` | number | ❌ NO | 사용자 ID |
| M | `order_item_id` | number | ❌ NO | 주문 아이템 ID |
| N | `order_id` | number | ❌ NO | 주문 ID |
| O | `country` | string | ❌ NO → 활용 권장 | 리뷰어 국가 |

**🔥 활용 방안**:
1. NPS 지표 산출: rating 기준 Promoter(9-10)/Detractor(1-6) 분류
2. 평점 트렌드: dt + rating 조합으로 시계열 분석
3. 작가별 평점: artist_id 기준 집계
4. 리뷰 키워드 분석: contents 일본어 NLP

---

### 10.9 Settlement_records 시트 컬럼 정의 (28개)

> **Raw Data 출처**: `[GB] 대시보드 제작 - Settlement_records.csv`  
> **Primary Key**: `id`

| 컬럼 | 컬럼명 | 타입 | 활용 여부 | 설명 |
|:----:|--------|------|:--------:|------|
| A | `id` | string | ✅ YES | 정산 레코드 ID (PK) |
| B | `period` | string | ✅ YES | 정산 기간 (YYYY-MM) |
| C | `uploaded_at` | datetime | ✅ YES | 업로드 일시 |
| D | `no` | number | ✅ YES | 정산서 순번 |
| E | `carrier` | string | ✅ YES | 운송사 (LOTTEGLOBAL, KPACKET, EMS) |
| F | `tracking_number` | string | ✅ YES | 해외운송장번호 |
| G | `shipment_id` | number | ✅ YES | 배송 ID (FK) |
| Q | `actual_weight` | number | ✅ YES | 실중량 (kg) |
| R | `volumetric_weight` | number | ✅ YES | 부피중량 (kg) |
| **S** | **`charged_weight`** | number | ✅ **YES** | **청구중량 (kg) - 비용 계산 기준** |
| **AA** | **`total_cost`** | number | ✅ **YES** | **운임 합계 (원) - 핵심 KPI** |

---

### 10.10 Coupon (쿠폰) 시트 컬럼 정의 (22개) 🆕

> **Raw Data 출처**: `[GB] 대시보드 제작 - coupon.csv`  
> **데이터 건수**: ~673건 (2024.01 ~ 현재)  
> **Primary Key**: `coupon_id`  
> **⭐ 신규 추가**: 쿠폰 효과 분석, ROI 측정, 프로모션 최적화에 활용

#### 기본 정보 필드 (A~F)

| 컬럼 | 컬럼명 | 타입 | 활용 가능성 | 설명 |
|:----:|--------|------|:--------:|------|
| A | `coupon_id` | number | ✅ YES | 쿠폰 고유 ID (PK) |
| B | `coupon_name` | string | ✅ YES | 쿠폰 이름 (예: idus WELCOME Coupon) |
| C | `description` | string | ✅ YES | 쿠폰 설명 (예: Valid on all items) |
| D | `month` | number | ✅ YES | 월 (1~12) |
| E | `from_datetime` | datetime | ✅ YES | 쿠폰 시작 일시 |
| F | `to_datetime` | datetime | ✅ YES | 쿠폰 종료 일시 |

#### 유효 기간 필드 (G~H)

| 컬럼 | 컬럼명 | 타입 | 활용 가능성 | 설명 |
|:----:|--------|------|:--------:|------|
| G | `max_valid_datetime` | datetime | ✅ YES | 최대 유효 일시 |
| H | `valid_period` | number | ✅ YES | 유효 기간 (일) |

#### 할인 정보 필드 (I~M) - **핵심 필드**

| 컬럼 | 컬럼명 | 타입 | 활용 가능성 | 설명 |
|:----:|--------|------|:--------:|------|
| **I** | **`discount_type`** | string | ✅ **YES** | **할인 유형 (RATE: 비율, FIXED: 정액)** |
| **J** | **`discount`** | number | ✅ **YES** | **할인 값 (비율 % 또는 금액 USD/JPY)** |
| K | `min_order_price` | number | ✅ YES | 최소 주문 금액 |
| L | `max_discount_price` | number | ✅ YES | 최대 할인 금액 |
| M | `issue_limit` | number | ✅ YES | 발급 한도 |

#### 발급/사용 정보 필드 (N~P) - **핵심 필드**

| 컬럼 | 컬럼명 | 타입 | 활용 가능성 | 설명 |
|:----:|--------|------|:--------:|------|
| N | `landing_scheme` | string | ❌ NO | 랜딩 URL 스킴 (앱 내 이동) |
| O | `created_at` | datetime | ✅ YES | 쿠폰 생성 일시 |
| **P** | **`issue_count`** | number | ✅ **YES** | **발급 수 - 쿠폰 도달률 분석** |

#### 성과 분석 필드 (Q~V) - **핵심 KPI**

| 컬럼 | 컬럼명 | 타입 | 활용 가능성 | 설명 |
|:----:|--------|------|:--------:|------|
| **Q** | **`used_count`** | number | ✅ **YES** | **사용 수 - 쿠폰 전환율 분석** |
| **R** | **`discount_amount`** | number | ✅ **YES** | **할인 금액 합계 (USD/JPY)** |
| **S** | **`total_gmv`** | number | ✅ **YES** | **쿠폰 사용 주문 GMV (USD/JPY)** |
| **T** | **`pay_gmv`** | number | ✅ **YES** | **실 결제 금액 (USD/JPY)** |
| U | `Coupon_type` | string | ✅ YES | 쿠폰 유형 (idus, CRM, Sodam 등) |
| **V** | **`coupon_country`** | string | ✅ **YES** | **쿠폰 대상 국가 (EN, JP)** |

#### 예시 데이터

```
coupon_id: 918, 959, 966
coupon_name: "配送遅延のお詫びクーポン", "WELCOME BACK Coupon", "今だけ！初回限定クーポン"
discount_type: "FIXED" (정액), "RATE" (비율)
discount: 1000 (JPY), 10 (%), 500 (JPY)
issue_count: 27, 789, 121
used_count: 3, 1, 2
total_gmv: 14142, 250.29, 5524
pay_gmv: 11142, 240.29, 4524
Coupon_type: "idus", "CRM", "Sodam"
coupon_country: "JP", "EN"
```

#### 🔥 Coupon 시트 분석 활용 방안

| 분석 항목 | 설명 | 공식/방법 |
|----------|------|----------|
| **쿠폰 발급률** | 캠페인 도달률 | issue_count / issue_limit × 100 |
| **쿠폰 전환율** | 발급 대비 사용률 | used_count / issue_count × 100 |
| **쿠폰 ROI** | 할인 대비 매출 효과 | total_gmv / discount_amount |
| **국가별 효과** | 국가별 쿠폰 성과 | coupon_country 기준 그룹화 |
| **유형별 효과** | CRM vs 프로모션 | Coupon_type 기준 그룹화 |
| **월별 트렌드** | 시즌별 쿠폰 효과 | month 기준 그룹화 |
| **할인 유형 비교** | 정액 vs 비율 | discount_type 기준 비교 |

---

### 10.11 컬럼 활용 현황 요약

| 시트 | 전체 컬럼 | 활용 컬럼 | 활용률 | 비고 |
|:----:|:--------:|:--------:|:-----:|------|
| Order | 27 | 17 | **63%** | 파생 컬럼 미활용 |
| **Logistics** | 43 | 38 | **88%** | ⭐ 최고 활용도 |
| Users | 7 | 3 | **43%** | 개인정보 미활용 |
| Artists | 20 | 11 | **55%** | KR 관련 미활용 |
| **Review** | 15 | 0 | **0%** | ⚠️ 완전 미활용 |
| Settlement | 28 | 22 | **79%** | 높은 활용률 |
| **🆕 Coupon** | 22 | 0 | **0%** | 🆕 신규 추가 - 활용 예정 |

---

## 📋 문서 변경 이력

| 버전 | 날짜 | 변경 내용 |
|:----:|------|----------|
| 1.0 | 2024-12-17 | 최초 작성 (기존 통합 문서에서 분리) |
| **1.1** | **2024-12-17** | **🆕 Coupon 시트 정의 추가 (22개 컬럼)** |

---

> **문서 관리자**: AI 자동화팀  
> **최종 업데이트**: 2024-12-17  
> **관련 문서**: `허브_개선_제안서.md` (개선 제안 및 로드맵)

