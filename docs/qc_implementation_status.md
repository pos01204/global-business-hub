# QC 관리 기능 구현 현황 및 이어서 작업 가이드

## 📋 프로젝트 개요

일본어 작품의 한글 번역 및 이미지 QC(품질 관리) 작업을 효율적으로 수행하기 위한 웹 기반 관리 시스템입니다.

### 주요 목표
- CSV 파일을 통한 QC 데이터 수집
- 텍스트 QC: 일본어 원문과 한글 번역 비교 검수
- 이미지 QC: 이미지와 OCR 결과 확인 검수
- 작가별 수정 필요 항목 집계 및 알람 명단 생성
- QC 완료 내역 아카이빙 및 중복 방지

---

## ✅ 현재 구현 완료 상태

### Phase 1: 기본 기능 구현 (완료)
- ✅ 사이드바에 "QC 관리" 메뉴 추가
- ✅ QC 페이지 생성 (탭 구성: CSV 업로드, 텍스트 QC, 이미지 QC, 작가 알람 명단, QC 아카이브)
- ✅ 백엔드 QC API 라우트 구현
- ✅ 프론트엔드 QC API 클라이언트 추가
- ✅ 작가 UUID → 작가명 매칭 로직 구현

### Phase 2: UX 개선 (완료)
- ✅ CSV 업로드 탭 구현
  - 텍스트/이미지 QC CSV 파일 업로드
  - 업로드 진행 상태 표시
  - 업로드 이력 표시 (가져온/스킵/중복 항목 수)
- ✅ 텍스트 QC 탭 구현
  - 일본어 원문/한글 번역 비교 뷰
  - 상태 필터링 (전체/미검수/수정 필요/승인 완료)
  - 주간 신규만 보기 옵션
  - 통계 카드 (전체/미검수/수정 필요)
  - 승인/수정 필요/완료 처리 기능
  - 페이지네이션
  - 키보드 단축키 (A: 승인, R: 수정 필요, C: 완료, N: 다음, P: 이전)
- ✅ 이미지 QC 탭 구현
  - 이미지 그리드 뷰 (2-4열 반응형)
  - 이미지 썸네일 자동 표시
  - OCR 결과 미리보기
  - 상태 배지 표시
  - 이미지 모달 확대 보기
  - 키보드 네비게이션 (← → 화살표, ESC)
  - 승인/수정 필요/완료 처리 기능
- ✅ 작가 알람 명단 탭 구현
  - 작가별 수정 필요 항목 집계
  - 텍스트/이미지 QC 항목 수 표시
  - 작가별 상세 항목 목록
  - 통계 카드 (작가 수, 항목 수)

### Phase 3: 아카이빙 및 중복 방지 (부분 완료)
- ✅ QC 완료 처리 API 구현
- ✅ 아카이브 저장 로직 구현
- ✅ 중복 검사 로직 구현
- ⚠️ QC 아카이브 탭 UI만 구현 (기능 미완성)

### Phase 4: 모니터링 및 통계 (미완료)
- ❌ QC 아카이브 조회 기능
- ❌ QC 통계 대시보드
- ❌ 작가 알람 발송 기능

---

## 📁 파일 구조

### 백엔드
```
backend/
├── src/
│   ├── routes/
│   │   └── qc.ts                    # QC API 라우트 (완료)
│   ├── config/
│   │   └── sheets.ts                # Google Sheets 설정 (QC 시트명 추가됨)
│   └── index.ts                     # 메인 서버 파일 (qc 라우트 등록됨)
└── package.json                     # csv-parse 패키지 추가됨
```

### 프론트엔드
```
frontend/
├── app/
│   └── qc/
│       ├── page.tsx                 # QC 메인 페이지 (탭 네비게이션)
│       └── components/
│           ├── CSVUploadTab.tsx     # CSV 업로드 탭 (완료)
│           ├── TextQCTab.tsx        # 텍스트 QC 탭 (완료)
│           ├── ImageQCTab.tsx       # 이미지 QC 탭 (완료)
│           ├── ArtistsNotificationTab.tsx  # 작가 알람 명단 탭 (완료)
│           └── QCArchiveTab.tsx     # QC 아카이브 탭 (UI만 완료)
├── components/
│   └── Sidebar.tsx                  # 사이드바 (QC 메뉴 추가됨)
└── lib/
    └── api.ts                       # QC API 클라이언트 (완료)
```

---

## 🔌 API 엔드포인트

### CSV 업로드
- `POST /api/qc/upload/text` - 텍스트 QC CSV 업로드
- `POST /api/qc/upload/image` - 이미지 QC CSV 업로드

### QC 목록 조회
- `GET /api/qc/text/list` - 텍스트 QC 목록
  - Query params: `status`, `page`, `limit`, `weeklyOnly`
- `GET /api/qc/image/list` - 이미지 QC 목록
  - Query params: `status`, `page`, `limit`, `weeklyOnly`

### QC 상태 관리
- `PUT /api/qc/:type/:id/status` - QC 상태 업데이트
  - Body: `{ status: string, needsRevision: boolean }`
- `POST /api/qc/:type/:id/complete` - QC 완료 처리 및 아카이빙

### 작가 알람
- `GET /api/qc/artists/notifications` - 작가 알람 명단 조회

### 중복 검사
- `POST /api/qc/check-duplicates/:type` - 중복 검사
  - Body: `{ data: any[] }`

---

## 📊 데이터 구조

### Google Sheets 시트명
```typescript
QC_TEXT_RAW: '1.설명글_한글_raw'        // 텍스트 QC 원본 데이터
QC_IMAGE_RAW: '1.OCR_결과_raw'          // 이미지 QC 원본 데이터
QC_TEXT_PROCESS: '텍스트 QC & 이미지 QC' // 활성 QC 처리 시트
QC_IMAGE_PROCESS: '텍스트 QC & 이미지 QC' // 활성 QC 처리 시트 (동일)
QC_ARCHIVE: '3.QC 완료 / 작가푸시'      // 완료된 QC 아카이브
ARTIST_MAPPING: 'artists'               // 작가 UUID → 작가명 매핑
```

### QC 항목 데이터 구조
```typescript
interface QCItem {
  id: string                    // 고유 ID (type_global_product_id 형식)
  type: 'text' | 'image'       // QC 타입
  data: any                     // 원본 CSV 데이터
  status: 'pending' | 'approved' | 'needs_revision'
  needsRevision: boolean
  createdAt: Date
  completedAt?: Date
}
```

### 텍스트 QC CSV 컬럼 (예상)
- `global_product_id`
- `name` (일본어 원문)
- `pdp_descr` (일본어 상세 설명)
- `product_name` (한글 번역)
- `korean_place` (한글 위치)
- `artist_uuid` (작가 UUID)

### 이미지 QC CSV 컬럼 (예상)
- `product_id`
- `product_name`
- `image_url`
- `detected_text` (OCR 결과)
- `page_name` (페이지 유형)
- `cmd_type` (명령 유형)
- `artist_id` (작가 ID)

---

## 🚧 남은 작업 목록

### 우선순위 1: QC 아카이브 기능 완성
**파일**: `frontend/app/qc/components/QCArchiveTab.tsx`

**필요 작업**:
1. 백엔드 API 엔드포인트 추가
   - `GET /api/qc/archive` - 아카이브 목록 조회
   - Query params: `type`, `page`, `limit`, `startDate`, `endDate`
2. 프론트엔드 구현
   - 아카이브 목록 표시
   - 날짜 필터
   - 타입 필터 (텍스트/이미지)
   - 검색 기능
   - 통계 표시 (완료된 항목 수, 기간별 통계)

**참고**:
- 아카이브 데이터는 `QC_ARCHIVE` 시트에 저장됨
- 완료 처리 시 자동으로 아카이브에 추가됨

### 우선순위 2: 작가 알람 발송 기능
**파일**: `frontend/app/qc/components/ArtistsNotificationTab.tsx`

**필요 작업**:
1. 백엔드 API 엔드포인트 추가
   - `POST /api/qc/artists/notify` - 작가에게 알람 발송
   - Body: `{ artistId: string, items: string[] }`
2. 프론트엔드 구현
   - 알람 발송 버튼 활성화
   - 알람 발송 확인 다이얼로그
   - 발송 이력 표시
   - 발송 상태 관리

**참고**:
- 실제 알람 발송 시스템과의 연동 필요 (이메일, SMS, 푸시 등)
- 발송 이력은 별도 시트에 저장 권장

### 우선순위 3: QC 통계 대시보드
**새 파일 생성 필요**: `frontend/app/qc/components/QCStatsTab.tsx` 또는 별도 페이지

**필요 작업**:
1. 백엔드 API 엔드포인트 추가
   - `GET /api/qc/stats` - QC 통계 조회
   - Query params: `startDate`, `endDate`
2. 프론트엔드 구현
   - 전체 QC 진행률 차트
   - 타입별 QC 통계 (텍스트/이미지)
   - 상태별 분포 차트
   - 기간별 완료 추이
   - 작가별 QC 통계

**참고**:
- Chart.js 또는 Recharts 사용 권장
- 기존 `frontend/app/analytics/page.tsx` 참고

### 우선순위 4: 아카이빙 및 중복 방지 로직 강화
**파일**: `backend/src/routes/qc.ts`

**필요 작업**:
1. 주간 신규 QC 목록 관리
   - 주간 기준일 설정 (예: 매주 월요일)
   - 주간 신규 항목 자동 마킹
   - 이전 주 항목 자동 아카이빙
2. 중복 방지 로직 강화
   - 아카이브 검색 최적화
   - 대량 데이터 처리 성능 개선
   - 인덱싱 전략 수립

---

## 🛠 기술 스택

### 백엔드
- Express.js
- TypeScript
- Google Sheets API (googleapis)
- Multer (파일 업로드)
- csv-parse (CSV 파싱)

### 프론트엔드
- Next.js 14 (App Router)
- React 18
- TypeScript
- Tailwind CSS
- React Query (@tanstack/react-query)
- Chart.js (통계 대시보드용, 필요시)

---

## ⚠️ 주의사항 및 알려진 이슈

### 1. 메모리 관리
- 현재 QC 데이터는 서버 메모리에 저장됨 (`qcDataStore`)
- 대량 데이터 처리 시 메모리 부족 가능성
- **해결 방안**: Redis 또는 데이터베이스 사용 고려

### 2. Google Sheets API 제한
- Google Sheets API는 분당 요청 수 제한이 있음
- 대량 데이터 처리 시 rate limiting 고려 필요

### 3. 파일 업로드 크기 제한
- Multer 기본 설정 사용 중
- 큰 CSV 파일 처리 시 메모리/타임아웃 이슈 가능

### 4. 키보드 단축키 충돌
- 텍스트 QC 탭의 키보드 단축키가 입력 필드에서도 작동할 수 있음
- 현재는 입력 필드 포커스 시 무시하도록 구현됨

### 5. 작가 매칭 로직
- 작가 UUID와 작가명 매칭이 여러 컬럼명을 시도하도록 구현됨
- 실제 데이터 구조에 맞게 조정 필요할 수 있음

---

## 📝 코드 스타일 가이드

### 컴포넌트 구조
```typescript
'use client'

import { useState, useEffect } from 'react'
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import { qcApi } from '@/lib/api'

export default function ComponentName() {
  // 상태 관리
  const [state, setState] = useState()
  
  // React Query
  const { data, isLoading, error } = useQuery({
    queryKey: ['qc', 'key'],
    queryFn: () => qcApi.method(),
  })
  
  // 렌더링
  if (isLoading) return <Loading />
  if (error) return <Error />
  
  return <div>...</div>
}
```

### API 호출 패턴
```typescript
// lib/api.ts에 추가
export const qcApi = {
  methodName: async (params) => {
    const response = await api.get('/api/qc/endpoint', { params })
    return response.data
  },
}
```

### 에러 처리
- 모든 API 호출에 try-catch 적용
- 사용자 친화적인 에러 메시지 표시
- 콘솔 로그로 디버깅 정보 기록

---

## 🔗 관련 문서

- `docs/qc_automation_design.md` - QC 자동화 설계 문서
- `backend/src/routes/qc.ts` - 백엔드 구현 코드
- `frontend/app/qc/page.tsx` - 프론트엔드 메인 페이지

---

## 📞 작업 시작 전 확인사항

1. ✅ Git 저장소 최신 상태 확인
2. ✅ 로컬 환경 변수 설정 확인 (Google Sheets API 키 등)
3. ✅ 백엔드 서버 실행 확인
4. ✅ 프론트엔드 개발 서버 실행 확인
5. ✅ Google Sheets 접근 권한 확인

---

## 🎯 다음 단계 작업 예시

### 예시 1: QC 아카이브 조회 기능 구현

```typescript
// backend/src/routes/qc.ts에 추가
router.get('/archive', async (req, res) => {
  try {
    const { type, page = 1, limit = 20, startDate, endDate } = req.query
    
    // Google Sheets에서 아카이브 데이터 조회
    const archiveData = await sheetsService.readSheet(
      SHEET_NAMES.QC_ARCHIVE,
      // 필터링 로직
    )
    
    res.json({
      items: archiveData,
      pagination: { page, limit, total: archiveData.length }
    })
  } catch (error) {
    res.status(500).json({ error: error.message })
  }
})
```

```typescript
// frontend/lib/api.ts에 추가
getArchive: async (params) => {
  const response = await api.get('/api/qc/archive', { params })
  return response.data
}
```

```typescript
// frontend/app/qc/components/QCArchiveTab.tsx 수정
const { data, isLoading } = useQuery({
  queryKey: ['qc', 'archive', typeFilter, page],
  queryFn: () => qcApi.getArchive({ type: typeFilter, page, limit }),
})
```

---

**마지막 업데이트**: 2025-01-XX
**작성자**: AI Assistant
**상태**: Phase 1-2 완료, Phase 3-4 진행 중






