# 프론트엔드 연결 오류 해결 가이드

## 🔍 오류: "ERR_CONNECTION_REFUSED" (localhost:3000)

이 오류는 프론트엔드 서버가 실행되지 않았을 때 발생합니다.

## ✅ 해결 방법

### 1단계: 프론트엔드 서버 실행

터미널을 열고 다음 명령어를 실행하세요:

```bash
cd frontend
npm run dev
```

**예상 출력:**
```
  ▲ Next.js 14.0.4
  - Local:        http://localhost:3000
  - Ready in 2.3s
```

### 2단계: 서버 실행 확인

브라우저에서 다시 접속:
```
http://localhost:3000
```

## 🔧 문제 해결

### 문제 1: "npm run dev" 실행 시 오류

**증상:**
- 의존성 오류
- 포트 이미 사용 중
- 빌드 오류

**해결:**

#### 1. 의존성 설치
```bash
cd frontend
npm install
```

#### 2. 포트 확인 (다른 포트 사용)
포트 3000이 사용 중이라면 다른 포트 사용:

```bash
# Windows PowerShell
$env:PORT=3001; npm run dev

# 또는 package.json 수정
# "dev": "next dev -p 3001"
```

#### 3. 포트 사용 중인 프로세스 종료
```bash
# Windows에서 포트 3000 사용 중인 프로세스 찾기
netstat -ano | findstr :3000

# 프로세스 ID 확인 후 종료
taskkill /PID <프로세스ID> /F
```

### 문제 2: 의존성 오류

**해결:**
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### 문제 3: 빌드 오류

**해결:**
```bash
cd frontend
npm run build
npm run dev
```

## 📋 체크리스트

- [ ] `cd frontend` 디렉토리로 이동
- [ ] `npm install` 실행 (의존성 설치)
- [ ] `npm run dev` 실행
- [ ] 터미널에 "Ready" 메시지 확인
- [ ] 브라우저에서 `http://localhost:3000` 접속

## 🎯 빠른 해결 (한 번에 실행)

```bash
cd frontend && npm install && npm run dev
```

## 💡 팁

### 백엔드와 프론트엔드 동시 실행

**터미널 1 (백엔드):**
```bash
cd backend
npm run dev
```

**터미널 2 (프론트엔드):**
```bash
cd frontend
npm run dev
```

### 포트 확인

- **프론트엔드**: `http://localhost:3000` (기본값)
- **백엔드**: `http://localhost:3001` (기본값)

두 서버 모두 실행되어 있어야 정상 작동합니다.

## 🚨 자주 발생하는 오류

### 1. "Cannot find module"
```bash
cd frontend
npm install
```

### 2. "Port 3000 is already in use"
```bash
# 다른 포트 사용
npm run dev -- -p 3001
```

### 3. "EADDRINUSE"
포트가 사용 중입니다. 다른 프로세스를 종료하거나 다른 포트 사용.

## 📝 참고

- Next.js는 기본적으로 포트 3000에서 실행됩니다
- 개발 모드에서는 `npm run dev` 사용
- 프로덕션 모드에서는 `npm run build` 후 `npm start` 사용




