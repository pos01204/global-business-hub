# 🔧 문제 해결 가이드

## ERR_CONNECTION_REFUSED 오류 해결 방법

### 원인 진단

`ERR_CONNECTION_REFUSED` 오류는 다음 중 하나일 수 있습니다:

1. **개발 서버가 실행되지 않음**
2. **컴파일 오류로 인해 서버가 시작되지 않음**
3. **포트 충돌**
4. **서버가 크래시됨**

---

## 해결 방법

### 1단계: 서버 재시작

터미널에서 다음 명령어를 실행하세요:

```bash
# 프로젝트 루트에서
npm run dev
```

### 2단계: 컴파일 오류 확인

서버를 시작할 때 다음과 같은 오류가 나타나면:

```
Error: x Unexpected token `div`. Expected jsx identifier
```

이는 `frontend/app/analytics/page.tsx` 파일의 구문 오류입니다.

### 3단계: 캐시 삭제

다음 명령어로 Next.js 캐시를 삭제하세요:

```bash
# frontend 폴더에서
cd frontend
rm -rf .next
# 또는 Windows PowerShell에서
Remove-Item -Recurse -Force .next
```

### 4단계: node_modules 재설치 (필요시)

```bash
# 프로젝트 루트에서
npm run install:all
```

---

## 현재 상태 확인

### 백엔드 서버 (포트 3001)
- URL: http://localhost:3001
- Health Check: http://localhost:3001/health

### 프론트엔드 서버 (포트 3000)
- URL: http://localhost:3000

---

## 빠른 해결 체크리스트

- [ ] 개발 서버가 실행 중인지 확인 (`npm run dev`)
- [ ] 터미널에 컴파일 오류가 있는지 확인
- [ ] `.next` 폴더 삭제 후 재시작
- [ ] 브라우저에서 http://localhost:3000 접속 시도
- [ ] 브라우저 콘솔에서 오류 확인 (F12)

---

## 추가 도움말

문제가 계속되면:
1. 터미널의 전체 오류 메시지를 확인하세요
2. 브라우저 개발자 도구(F12)의 콘솔 탭을 확인하세요
3. 네트워크 탭에서 요청 상태를 확인하세요


